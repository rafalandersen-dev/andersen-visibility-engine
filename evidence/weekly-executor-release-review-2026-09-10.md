# P3 weekly executor — implementation candidate, not released

The P0/P1/P2 releases remain preserved at main `7f5120291ff35eba36b75ae3baa6e4cb900095d0`. This candidate continues the P3 groundwork from `f6e44a49ac9ac89309490e55599f2cf67493fa22` in the single active checkout `/Users/rafi/.codex/worktrees/e25d/milo-growth-generation-result-recovery-20260909`, branch `codex/milo-weekly-executor-20260910`. The predecessor and `/tmp/milo-p3-weekly-20260910` are audit/preservation checkouts; do not implement concurrently there.

## Integrated execution

The existing authenticated scheduler endpoint has explicit monthly and weekly dispatch. Monthly skips weekly/paused projects before lease admission and summary email. A proposed database cron selects one weekly project fairly every five minutes; it never switches a project or funds a budget. Weekly uses the existing shared lease/coordinator, project-local preparation time, current/next-week recovery, and exact publication instants. At most one paid research/content/image stage is admitted per visit. Direct native OpenAI admission, result allowance and provider expense/permit gates remain separate and mandatory.

Research is retained with a pinned owner opportunity or explicitly labelled generated hypothesis, source observation provenance, accepted knowledge references, fact dependencies and immutable context hashes. Generation rechecks the selected context. Content and required image use server-allocated once-only request/output IDs and independent archives. Retained stages resume without provider replay. Delivery evidence is recorded in the same transaction as workspace insertion; deletion stays distinguishable from incomplete delivery. Owner edits, changed knowledge, cancellation, uncertain research and lost acknowledgements hold instead of authorizing paid replays or restoring owner-deleted output. Proposed generated visuals remain private and require review; no synthetic score approves pixels.

Exact automatic publication now rederives the saved version and blockers, rechecks current explicit autopilot authority, and commits approval plus queue insertion under the same workspace revision/lease lock. It cannot revive withdrawn approval or queue competing dates. Weekly owner-reviewed completed content requires an existing exact approval before automatic queue admission. Existing connector final source/entitlement/transport and destination checks remain authoritative.

Four-language weekly UI shows actual durable research/content/image stages, owner-edit holds, cancellation and deduplicated in-app progress. Cancellation preserves drafts/archives and reserves the slot; it refuses active workers or an active publication queue. Expired uncertain content/image can reconcile only against the exact independent archive; unknown research needs explicit cancellation and never reruns automatically.

## Proposed migrations and rollback

All P3 migrations are **UNAPPLIED**: `20260910150000_weekly_preparation.sql`, `20260910170000_publication_approval.sql`, `20260910180000_weekly_executor.sql`, `20260910190000_weekly_dispatch.sql`. Apply only the final reviewed bytes, transactionally and once, before the corresponding code deployment. Never edit/reapply released P0/P1/P2 SQL.

Rollback: unschedule only `weekly-preparation` to stop new weekly visits; retained data stays private and recoverable. Explicit coordinator rollback to monthly requires all active/uncertain work to be resolved. Do not delete leases/stages or reset identities. An already-in-flight native request cannot be assumed cancelled; retain its archive and expense uncertainty. Existing monthly job remains unchanged, and its lease cannot acquire weekly projects. Do not revert the approval gates independently of their atomic scheduling integration.

## Verification and limits

Local full suite passed **2,591 tests / 188 files** before the additional six approval-boundary tests and final provenance/calendar refinements. The next exact-candidate full run supersedes this intermediate count. Types, focused lint and production build pass. Focused **30 tests / 3 files** cover executor orchestration, exact scheduler approval and actual PGlite migration execution, including two slots, retained partial work, no paid replay, owner edits/deletion, cancellation and summary dedupe. Single-session PGlite results are not multi-connection concurrency or live-provider acceptance. Logs `/tmp/milo-weekly-full-tests.log`, `/tmp/milo-weekly-final-focused.log`, `/tmp/milo-weekly-sql-tests.log`, `/tmp/milo-weekly-build.log`, `/tmp/milo-weekly-types.log`, `/tmp/milo-weekly-lint.log`.

No P3 PR/review/migration/deployment acceptance is claimed at this writing. No live provider generation, funding, source production fetch, email/outreach/client publication, key setup, authentication retry, browser-policy bypass, subagent or new Codex automation. Weekly funding remains unauthorized. Current account/provider/browser/Stripe restrictions and R00–R24/D01–D08 continue. P4/P5 follow P3 review/release; they are not complete.

Candidate validation now passes **2,597 tests / 189 files**, TypeScript and focused lint (`/tmp/milo-weekly-candidate-full-tests.log`, `/tmp/milo-weekly-types.log`, `/tmp/milo-weekly-candidate-lint.log`). Production build passed; removing an unreachable post-visual auto-queue branch afterward leaves the explicit exact-owner-approval finalization path as the sole weekly queue path. Source provenance now retains actual scoped knowledge/fact version references in addition to their immutable hashes. Current-week selection checks real queue state so queued content without a UI mirror cannot starve next-week preparation. Cancelled stage reservations appear as cancelled in readiness and are excluded from missing-work admission.

## First independent review corrections (not yet released)

PR #110 opened at `19e16fd8190675e020dcb1ee673833cf9b79b3bc`. Linux matrix `34474882685` passed on Bun 1.3.3/1.4.0. Initial Codex code review completed with three actionable findings: monthly capacity demand must exclude weekly projects; a coordinator read must stay inside each monthly project's failure boundary; cancelled preparation must remain visibly cancelled even when its draft is retained. All three are corrected locally with regressions. Paused projects also stop cadence-demand alerts, unavailable coordinator evidence preserves the inbox, and coordinator changes bump the workspace revision to invalidate stale notification snapshots. A bounded account-scoped control RPC avoids per-project notification query fanout. Active/published queue state still takes precedence over old preparation cancellation if the owner later explicitly schedules the retained draft.

Additional local audit corrections reject discarded/rescheduled/already-drafted pinned opportunities before content admission. Weekly workspace/context/lease operations now have explicit ten-second bounds; paid stage admission closes after sixty seconds and content/image cores have an outer 180-second uncertainty bound in addition to native provider limits. A timeout never cancels or retries an unknown provider operation; exact private archive recovery remains available. Focused 55 tests / 4 files pass for these boundaries (`/tmp/milo-weekly-review-corrections-tests.log`); SQL and notification tests pass 33 / 2 files. Security review of the first candidate remains running. All P3 migration/deployment operations remain held.

The review-correction full suite passes **2,605 tests / 189 files** (`/tmp/milo-weekly-followup-full-tests.log`). An additional queued-current-week regression and the final paused-config normalization then pass focused **31 tests / 2 files**; final types and focused lint pass (`/tmp/milo-weekly-followup-types.log`, `/tmp/milo-weekly-followup-lint.log`). Initial security review is still in progress; no migration/deployment has run.

Second Codex review completed 12:24:04 UTC on `1fc986d`, finding one additional P1 admission gap: manual scheduling still accepted status-only approval and would fail later at publication. Manual scheduling now verifies exact saved approval and deterministic blockers before any queue mutation. A new service-only SQL RPC checks the same workspace revision/approval and atomically cancels the previous pending row and inserts its replacement; an in-flight row or failed/stale approval preserves the original schedule. The existing four-locale approval messages are used by calendar/editor scheduling. Focused 26 tests / 3 files pass, including actual SQL preservation/atomic replacement and server no-mutation-on-failure tests; types/lint pass. P3 is still unapplied/unreleased. Initial security review finished on `19e16fd` with no additional posted findings. The corrected `1fc986d` Linux matrix `34475891557` passed **2,606 tests / 189 files**, types/builds on Bun 1.3.3/1.4.0. The new manual-admission candidate needs its own review and Linux checks.

Manual-admission candidate full local validation passes **2,611 tests / 190 files**, types and focused lint (`/tmp/milo-p3-manual-approval-full-tests.log`, `-types.log`, `-lint.log`).

Deployment-order correction: `20260910190000_weekly_dispatch.sql` now creates the weekly timer **disabled** in the same transaction. The previous production route ignores `?engine=weekly`; activating the timer before new code could otherwise invoke the monthly runner. Production pg_cron `alter_job(job_id bigint, schedule text, command text, database text, username text, active boolean)` availability was verified read-only. The migration's disabled timer is covered by an actual PGlite execution test with cron functions stubbed; 17 migration tests pass. Release sequence is now: apply guarded SQL once → merge/deploy exact reviewed code → verify runtime identity and anonymous authorization boundary → explicitly activate only the exact reviewed weekly timer. A guarded activation script must verify the stored migration and timer-command hashes. No weekly project is switched or funded. The timer remains inactive through any failed deployment.
