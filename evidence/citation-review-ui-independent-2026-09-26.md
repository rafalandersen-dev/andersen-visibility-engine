# P4 independent review — 26 September 2026

Status: candidate reviewed locally; NOT deployed. Claude authored implementation/corrections/tests/translations; Codex independently reviewed security and integration, exercised the real component in a browser, and ran full candidate validation. Base: bab861c872e4ade3a4563d279b1a68ac674d24b7. Worktree recovery and generated route-tree integration were minimal Codex integration exceptions; published history was not rewritten.

## Verification

- Full Vitest suite: **6707 tests / 394 files PASS**, 70.23s (Codex, final candidate).
- Production build: **PASS** (Codex, same sequential run, exit0).
- Types, candidate Prettier and scoped ESLint: **PASS**, 0 warnings (Claude's completed final packet); catalog/staged/focused tests **778 / 33 files PASS**. Global lint is not claimed clean.
- Independent review: scoped actor/owner/project/finding queries; immutable hash-bound decisions; incomplete evidence cannot be approved; denied finding material stays hidden during own-receipt withdrawal. Every new reviewer mount waits for its own authorized read using unique query keys with immediate unobserved cache collection. A remount cannot reveal old cached data before authorization.
- Parent routing: only exact `/app/citation-review` added to existing onboarding exemptions. Authentication, hydration/error guards and preserved sign-in destination remain intact. A reviewer need not create an owned project to reach the child route.
- Localization: 124 new keys in active en/pl/sv/da and all20 staged catalogs; staged runtime availability unchanged. Source fingerprints and parameter coverage retained. Adjusted expected total3984 and narrow identical technical-token allowances are legitimate additions, not a coverage bypass. All23 non-English batches are machine-authored; fluent acceptance remains pending.

## Actual browser interactions (isolated fixtures)

The local harness mounted the real component, React Query, Radix and styles, replacing auth and server-function boundaries with deterministic in-memory fixtures. No production writes or provider calls.

Verified readable observation/hypothesis/support/source passages/dated facts; owner human judgement versus current resolution; explicit keyboard selection; incomplete-approval and masked-record blocking; same-hash conflict and changed-hash form reset; forbidden save followed by withdrawal start/failure/success without re-exposing data; remount with delayed4s refusal showing only loading then unavailable; background read failure hiding data and successful authorized reread restoring it; owner true-empty/list-error/detail-error states; eligible grants under reviewer-only and editor-enabled policies; inactive member revocation option; grant returns full current-origin scoped URL. Tested390x844 viewport,360px stage (358px content/scroll widths), select Escape/arrow/Enter and Tab focus.

Two independently discovered privacy defects (withdrawal clearing the denial, and remount reusing sensitive cache) and a parent onboarding redirect defect were corrected and rechecked. Earlier full-suite23failures from missing locale coverage were corrected; final6707tests all pass.

## Boundaries and remaining acceptance

Fixtures prove UI reactions, not production authorization or a real two-person journey. Parent routing has code and regression evidence; a browser journey using a real non-admin reviewer without owned projects remains open. No full screen-reader or fluent-language acceptance claim. Fresh reload after revocation cannot discover a receipt id for withdrawal; owner assignment inventory and reviewer inbox need separate narrow endpoints. Findings authoring, dated-fact UI, authenticated panel binding, independent destination proof, native parser based on genuine exports, approved manual pilot, comparable retest and full launch acceptance remain required. No applied SQL was modified/replayed. USD50 monthly AI and manual budget grants for free accounts remain unchanged.
