# P2 source refresh — work in progress

P1 release review continues separately in the main task worktree. P2 checkout `/tmp/milo-p2-source-refresh-20260910`, branch `codex/milo-p2-source-refresh-20260910`, starts from P1 candidate `ebd61fe`. No P2 migration, deployment, provider invocation, browser acceptance or client publication.

## Implemented foundation

`source-refresh.ts` defines bounded account/project-scoped source facts, snapshots, exact output dependencies, change comparison and a publication-time freshness evaluator. Partial/public observations cannot establish removal. Only successive complete catalog enumerations establish removal. Failed attempts are supplied separately and never replace a last successful observation. Product/variant/market/currency identity is checked alongside exact fact fingerprints. Changed/unknown/stale/expired facts return explicit issues without rewriting assets, changing schedules, retrying providers or publishing. Unrelated changed facts do not invalidate an unchanged dependency.

Fourteen deterministic tests pass, including changed price, offer expiry at the scheduled time, missing catalog coverage, source outage, future/stale observations, cross-project state and duplicate source identity. TypeScript and focused lint pass. Logs `/tmp/milo-p2-refresh-tests.log`, `/tmp/milo-p2-types.log`. This is not an integrated P2 product or release candidate.

The bounded public-page parsing adapter is now implemented and tested (22 tests across both modules; types and focused lint pass). It extracts explicit Product/Offer identity, names/descriptions/specifications, price with explicit currency, reported availability and offset-qualified expiry from Schema.org JSON-LD, plus a bounded readable-text excerpt. It does not execute page scripts, fetch linked data, infer missing IDs/currencies/markets/timezones, resolve conflicting offers, or treat AggregateOffer ranges as exact prices. Date-only expiry is retained as unknown validity and holds dependent use. Source definitions checked against https://schema.org/Product and https://schema.org/Offer. Network capture/persistence integration remains next; no live source fetch ran.

Proposed **unapplied** migration `20260910100000_source_refresh.sql` now adds a service-RPC-only state table linked by cascading foreign key to P1 sources. A two-minute lease and ten-minute per-source cooldown bound refresh admission; ten stored sources/project and bounded snapshot/fact sizes limit storage. Failures retain the last successful snapshot but explicitly mark status unknown. Source replacement clears old evidence/acceptance; revoke prevents reads and late writes; forget cascades. Successful refresh retains only unchanged accepted fact hashes. Review decisions check the expected snapshot and prior same-fact acceptance state, so competing owner decisions conflict instead of overwriting. No direct service table grants or browser role access.

All **32 tests / 3 files**, TypeScript and focused lint pass for this groundwork (`/tmp/milo-p2-all-focused-tests.log`, `/tmp/milo-p2-types.log`), including actual PostgreSQL migration execution through PGlite. No full-suite/build/release review has run for P2. No P2 server handlers, network orchestration, UI, generation integration, publication gate, authenticated catalog adapter or history/coverage presentation exists yet. SQL stores the current/last-success state, not a bounded history; integrate durable reviewable changes/history before claiming P2 complete. Published P1 metadata will be merged into this branch before handoff; P0/P1 must not be repeated.

## Next integration

1. Add a bounded public-page adapter using the existing pinned-address reader. Source-reported Schema.org Product/Offer fields must preserve explicit identity/market/currency, uncertain date-only offer expiry and conflicting offers. Never infer catalog completeness from HTML or execute page content. Existing Shopify support is article publishing, not authenticated catalog ingestion; do not claim the latter exists.
2. Persist configured sources, attempt/last-success metadata and bounded snapshots transactionally with project authorization, concurrent refresh leases/cooldown, compare-and-set revisions, replay identity and retained failure state. Revoke/forget/project deletion must remove future retrieval and retained private evidence under P1 rules. Add actual SQL isolation/concurrency tests before proposing a migration.
3. Integrate reviewable change/coverage/last-checked UI in all four locales; connect selected accepted facts to bounded generation context and durable exact output references. Distinguish source-reported facts, acceptance and independent verification. Keep source conflicts visible; no last-value-wins resolution.
4. Identify affected saved drafts from exact dependencies without touching owner text, unrelated drafts, approvals or schedules. Persist reviewed input/source versions. Add pre-publication recheck/holds through every existing manual/scheduled connector authorization path; changed assets require the new version's approval. P3 owns weekly coordination/cutover.
5. Full review/checks, migration-before-code release and exact runtime verification follow P1's established process. Signed-in browser/provider acceptance remains pending under inherited restrictions. R00–R24/D01–D08 and P3–P5 remain active; no new spend, key setup, outreach or publication authorization.

## P2 integration checkpoint — 10 September, successor worktree

The sequential handoff was accepted at exact pushed checkpoint `56cbe3853aea8be87847a812112f5b1453d3c275`. Active checkout is `/Users/rafi/.codex/worktrees/bbc0/milo-growth-generation-result-recovery-20260909`, branch `codex/milo-p2-integration-20260910`. The predecessor checkouts remain untouched. P1 release operations have not been repeated.

Implemented after the groundwork:
- Authenticated handlers derive the owner from middleware. Refresh RPC admission verifies project/source identity before the pinned-address public reader. One configured source per request; database leases/cooldowns remain authoritative; capture failure records unknown and preserves last success. RPC responses, scope, histories and accepted hashes are validated before use.
- The UNAPPLIED P2 migration now retains five prior successful observations and twenty recent acceptance/withdrawal decisions. Bounded parser warnings/conflicting keys are stored. Replacement clears previous evidence; revoke blocks retrieval and late writes; forget removes source evidence. Public/partial absence remains unconfirmed.
- Project Setup shows coverage, last attempt/success, current/prior facts, exact changed/unobserved fields, warnings/conflicts, acceptance controls and affected drafts. All controls/help are in EN/PL/SV/DA. Affected-draft assessment is bounded to 100 drafts and discloses additional drafts; it evaluates future planned publication times. No text, approval or dates are automatically rewritten.
- Current accepted source facts enter the shared bounded text/image generation context. Exact source/key/hash, original source/snapshot revisions and variant/market/currency references flow through content, images, editor saves, scheduler, owner runner and durable archive/recovery. Matching explicit identities with contradictory observations are held; SKU/URL/GID aliases are not guessed.
- A private dependency registry is written by a trigger in the generation-archive insertion transaction. Client saves and archive discard cannot erase it. Per-project capacity is 1,000 output identities; only identities/hashes are retained; project/asset deletion purges entries. Manual WordPress/Shopify/custom and scheduled publication re-read this registry, perform bounded refresh admission and check accepted/current exact facts before transport. Unknown/stale/changed/withdrawn facts hold. Source holds stop automatic retries while retaining the requested date and owner text/approval.
- Optional authenticated Shopify catalog source uses only an existing project connection. Fixed read-only GraphQL at `2026-07`, strict canonical `*.myshopify.com` destination, no redirects/cookies/retries, eight-second deadline and 300,000-byte response cap. Reads at most 20 variants/100 facts. Completeness is asserted only when that enumeration ends; larger catalogs are partial. Default-shop currency is explicit; no country-price, discount, channel or location-specific inventory verification is claimed. Missing permissions/access yields unknown. No real Shopify connection was invoked.

Technical source definitions checked against [Shopify ProductVariant](https://shopify.dev/docs/api/admin-graphql/latest/objects/ProductVariant) and [Shop](https://shopify.dev/docs/api/admin-graphql/latest/objects/Shop), including product-read permission, default shop currency and bounded variant pagination. Public Schema.org adapter source references remain above.

Verification before the final diagnostics/test additions: full local suite **2,488 tests / 175 files**, TypeScript and production build pass. Focused lint passes. Logs `/tmp/milo-p2-integration-tests.log`, `/tmp/milo-p2-integration-types.log`, `/tmp/milo-p2-integration-build.log`, `/tmp/milo-p2-integration-lint.log`. Latest focused run is logged separately at `/tmp/milo-p2-integration-focused.log`; refresh final exact-head checks before release. Linux Bun matrix workflow now also covers source/migration changes, so P2 receives both 1.3.3 and 1.4.0 checks without modifying dependencies.

Not yet released: P2 SQL is still UNAPPLIED, independent review and Linux CI are pending, and no P2 runtime verification exists. Real browser and provider/Shopify acceptance remain unavailable/unperformed under inherited restrictions. P3 version-bound weekly coordination/monthly cutover, P4 truthful roles/faces and learning, P5 outcomes/evaluations and all R00–R24/D01–D08 remain active. No provider call, funding, key setup, email, outreach, client publication, browser-policy retry or account change occurred.

### PR #109 review corrections

P2 candidate `599b286` passed Linux Bun 1.3.3/1.4.0 matrix `34461611853` (frozen locks, full suite, types and builds). Codex security review completed 09:42:04 UTC; code review completed 09:43:08 UTC with two findings. Both are fixed: retained contradictory observations continue to hold dependent generation/publication when their latest capture is unknown, and combined article/image references are evaluated in 100-item batches up to the supported 3,100 aggregate rather than applying a smaller 100-reference combined limit. New tests reproduce both conditions, including the affected-state UI model and missing browser dependency protection. **28 focused tests / 2 files, types and focused lint pass** (`/tmp/milo-p2-review-fixes-tests.log`, `/tmp/milo-p2-review-fixes-types.log`). No SQL change for these fixes. Final candidate review/matrix remain required.

Read-only production preflight confirms P1 registry present; both P2 tables and P2 registry absent; knowledge sources and archived generation results remain zero. P2 remains unapplied. P3 pure weekly planner/cutover groundwork is separately preserved in `/tmp/milo-p3-weekly-20260910`, `codex/milo-p3-weekly-20260910`, commit `7242a9e`, with 13 passing tests; do not deploy that unfinished packet with P2.

### Successor final review compatibility correction

Corrected head `32cc718` passed Linux matrix `34462441177` on both Bun versions. Exact-head Codex review completed 09:52:05 UTC and found an additional compatibility issue: the image limit rejected legacy assets without source dependencies. The limit now counts only images carrying source references, preserving the 3,100 aggregate evaluation bound while allowing larger unrelated image collections. Regression coverage includes legacy 31-image assets, mixed referenced/unreferenced collections, unavailable evidence and bounded source-image capacity. **15 publication tests, TypeScript and focused lint pass** (`/tmp/milo-p2-image-bound-tests.log`, `/tmp/milo-p2-image-bound-types.log`). SQL remains unchanged/unapplied. Claude workflow `34462441193` reported success with 15 permission denials and no buffered inline comments; not counted as substantive review. Production preflight remains source/archive/new-table/registry absent or zero; native budgets/permits/requests/receipts zero and historical usage 10 rows/226 units.

Final candidate `b7a0318` passed both Linux jobs (`34463171505`). Its Codex review completed 09:59:11 UTC with one storage-capacity finding: empty source dependency arrays consumed registry rows. The unapplied migration now returns without registry insertion for empty arrays, just as for absent arrays. Actual SQL regression tests verify ordinary content/image archives retain successfully without consuming capacity, including an already-full registry. **28 tests / 2 files, TypeScript and focused lint pass** (`/tmp/milo-p2-empty-registry-tests.log`, `/tmp/milo-p2-empty-registry-types.log`). This SQL change supersedes the previously recorded migration hash; recompute the final file before application. No migration or deployment has run.

Candidate `c2f72a8` passed Linux matrix `34463841128`. Its review completed 10:06:34 UTC with a schedule-state finding. A terminal source hold now clears the active `scheduledPublishAt` mirror while preserving the intended date separately as `sourceHeldPublishAt`, shown in all four editor locales as held and not queued. Retryable failures retain active schedules; new schedules and successful/cancelled cleanup clear the old held marker. Original text/approval and prepared monthly-draft reservations remain unchanged. Tests distinguish actual publication occupancy from already-delivered draft capacity. **56 tests / 3 files, TypeScript and focused lint pass** (`/tmp/milo-p2-held-date-tests.log`, `/tmp/milo-p2-held-date-types.log`). Prior empty-registry regression count was 29, not 28. SQL hash remains `46efd21781eee06991578f454d1e4f6e3a4f217d3fdc0c2367f1ffcaf2993cd2`; still unapplied.

Review of `dcc54d5` completed 10:17:09 UTC with two comments. Normal refresh calls already serialize through `assert_knowledge_project(...,true)` and its account-row lock; source admission now also explicitly locks the source row before checking/upserting refresh state and samples the timestamp after acquiring it. Regression tests verify a competing token cannot acquire or finish the winner's refresh (single-session PGlite, not a multi-connection concurrency claim). Manual connector success now clears active/failed/held schedule metadata through the common `markContentAssetPublishedLive` store action; save/reload regression proves the stale held notice disappears while article content and live URL survive. **48 tests / 3 files, TypeScript and focused lint pass** (`/tmp/milo-p2-final-cleanup-tests.log`, `/tmp/milo-p2-final-cleanup-types.log`). One existing store formatting violation was normalized while linting the changed file. Final unapplied SQL SHA256 is now `6aab37db6dd5acb211d14b944384cebb2c60de827c2114cc2b68425b4e9ff889`. All earlier hashes are superseded. No migration/deployment or provider activity.

Review of `3f9b737` completed 10:26:24 UTC with a source-forget privacy finding. The unapplied migration now redacts forgotten-source references from the dependency registry and retains only an output-level `source_forgotten` boolean; affected publication/impact paths hold even when browser dependency fields are absent. Source/product identifiers, keys and fingerprints are absent from the redacted registry response. A source-deletion trigger performs the cleanup, and late generation archives consult P1 source tombstones so they cannot reintroduce those registry identities. Other generated content itself is retained under the existing draft/archive retention rules. Minimal output hold rows remain subject to the 1,000-output bound to prevent unbounded tombstone storage; deleting the asset/project purges them through the existing cleanup path. **47 tests / 3 files, TypeScript and focused lint pass** (`/tmp/milo-p2-forget-tests.log`, `/tmp/milo-p2-forget-types.log`). Final SQL SHA256 `8925a8c0b8e6f9ae4fec433a61a5e42f23c579fd4a4e5fff89619475ae70591f` supersedes prior hashes. Eight P2 functions now exist in the proposed SQL: five service RPCs and three trigger-only functions. Still no migration or deployment.

Review of `40b55fe` completed 10:41:15 UTC with two compatibility/bounded-read findings. Catalog configuration and capture now share the existing connector domain normalizer, then enforce the strict Shopify destination schema before token resolution or transport. Existing short shop names and URL/path forms work; foreign, credential-bearing and port destinations remain rejected. Impact selection now scopes and caps workspace assets at 100 before the database request, passes explicit asset IDs to the registry RPC, and still inspects assets whose browser references are missing. Remaining counts include all uninspected project assets; empty projects make no registry request. The unapplied RPC accepts a maximum 100-ID filter while preserving existing single-asset calls. **67 tests / 4 files, TypeScript and focused lint pass** (`/tmp/milo-p2-final-bounds-tests.log`, `/tmp/milo-p2-final-bounds-types.log`). Final SQL SHA256 `305bf63aba7cdb049d92c6bba2b4c6aeb1a0d2a90c288e627f7ac95ba94d8198` supersedes earlier hashes. No migration, deployment or provider activity.

Review of `5e3b3e6` completed 10:53:09 UTC with source-refresh timing and revision cooldown findings. Publication now starts at most ten referenced source refreshes concurrently under one 20-second authorization deadline, checks expiry before subsequent phases, and never reaches connector transport after a failed/unconfirmed check. Already-started bounded read-only captures may finish retaining their observations, but cannot publish. Scheduled publication now claims one row per five-minute tick (including callers asking for a larger batch), leaving remaining due work unclaimed for later ticks and reserving the rest of the 110-second request window for transport/outcome recording. This deliberately lowers queue throughput rather than stranding preclaimed work; larger-scale throughput needs a separate bounded runner design. Tests exercise ten stalled sources together, deadline failure recording and the one-row claim. Source cooldown/lease reuse now applies only to the same source revision; replacing a source immediately admits fresh observation and clears prior evidence/approvals. **37 tests / 3 files**, types and focused lint pass (`/tmp/milo-p2-refresh-deadline-tests.log`, `/tmp/milo-p2-refresh-deadline-types.log`, `/tmp/milo-p2-refresh-deadline-lint.log`). Final unapplied SQL SHA256 `30a314afd1a2f57caf8d466541e6c9067c664d0efe18c792f7736399afa470c9` supersedes prior hashes. No migration, deployment or provider activity.

Review of `6401041` completed 11:05:21 UTC with a global queue-capacity finding. The runner now restores the existing default/maximum of 20 claims and starts every claimed row concurrently, so source deadlines overlap instead of multiplying across a serial batch. Each row retains independent failure/success recording; one held source cannot block another user's ready article. The batch heartbeat waits for all rows. Tests stall all twenty publications and prove all begin before any completion, verify all outcomes, and cover mixed held/ready rows and caller clamping. **22 tests / 2 files**, TypeScript and focused lint pass (`/tmp/milo-p2-concurrent-publish-{tests,types,lint}.log`). SQL remains unchanged at SHA256 `30a314afd1a2f57caf8d466541e6c9067c664d0efe18c792f7736399afa470c9` and unapplied. The prior one-row throughput statement is superseded. No migration, merge, deployment or provider call.
