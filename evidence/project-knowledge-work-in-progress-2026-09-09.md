# P1 project knowledge — implementation checkpoint

Updated 9 September 2026. **WIP: local implementation, not release-reviewed, migrated or deployed.** The successor accepted the handoff and fast-forwarded exactly to pushed foundation `d4490b13fa7167d9a2a11608a4f7bb34a74e2666`. Active worktree is `/Users/rafi/.codex/worktrees/72f1/milo-growth-generation-result-recovery-20260909`, branch `codex/milo-knowledge-continuation-20260909`. The original P1 tree stays preserved; do not implement there concurrently. No P1 PR exists at this writeback. The full [P1–P5 specification](../product/AGENT_WEEKLY_PLAN_2026_09_09.md) and R00–R24/D01–D08 remain active.

## Product completion — 10 September 2026

Active implementation is `/Users/rafi/.codex/worktrees/feb9/milo-growth-generation-result-recovery-20260909`, branch `codex/milo-p1-product-review-20260909`. The latest owner instruction is to continue without routine pauses, stopping only for a user-required action such as sign-in. Earlier worktrees are audit checkpoints, not concurrent implementation locations.

- Mapped editable proposals now recognize explicit text labels for six existing canonical brand fields (tone, writing style, preferred/avoided words, forbidden claims and required caveats). Other passages can be mapped manually. This is deterministic text mapping, not AI summary, OCR, visual extraction or verified factual learning.
- Accepted source-backed values fill empty, unowned canonical fields as a derived layer. No source values are copied into stored owner settings. Current source versions, expiry/conflict status and prompt bounds determine the actual generation contribution. Revocation/replacement/forget removes it; owner values and explicit clears survive.
- Canonical ownership is shared with MCP fill/approved setup paths. Brand form saves merge only changed fields and refuse same-field conflicts. Review and source-history controls, statuses/errors and new onboarding controls are translated into the four supported UI locales; broader R20 remains open.
- Onboarding drafts are account-keyed and retain the saved project identity/baseline across refreshes. Later owner edits survive wizard saves, review completion does not replay old profile values, duplicate service entries are prevented, and a synchronous generation guard blocks repeat submission.
- Latest full suite: **2,413 tests / 169 files pass**. TypeScript and focused lint pass. Production build passes; a clean candidate build follows checkpointing. Logs use `/tmp/milo-product-*`. Browser acceptance is still unavailable and is not claimed.
- Read-only live preflight confirms P1 objects/migration absent and the P0 recovery migration installed. No migration, deployment, paid call, email, client publication, account change or browser attempt occurred.
- The [P1 release-review packet](project-knowledge-release-review-2026-09-10.md) records architecture, verification, preflight, migration hash/order, rollback and remaining acceptance limits. Next: independent PR review, address findings, migration/runtime release verification, then P2–P5 under R00–R24/D01–D08.

## Product-review continuation checkpoint

The product-review successor accepted the handoff and fast-forwarded exactly to pushed checkpoint `c055a1e7bb77c5be53474df8878357bf3cbe972b`. Active worktree is `/Users/rafi/.codex/worktrees/feb9/milo-growth-generation-result-recovery-20260909`, branch `codex/milo-p1-product-review-20260909`. The previous tree is an audit checkpoint; do not implement there concurrently.

- Record history now exposes older pages and a return to the latest saved versions. Source history also supports return to latest. Empty older pages are explicit, and close buttons are disabled during history requests.
- New pagination labels are translated into English, Polish, Swedish and Danish through the existing i18n system. Remaining knowledge/onboarding controls still need localization; this is not full P1 or R20 acceptance.
- Added actual migration regression coverage for 42 record revisions: complete descending pages without overlap/gaps, empty end, account isolation, oldest-version restore with compare-and-set protection, and forget removing history. Focused verification passes **27 tests / 3 files**, TypeScript, focused lint and diff whitespace checks. Logs: `/tmp/milo-product-history-tests.log`, `/tmp/milo-product-types.log`, `/tmp/milo-product-lint.log`. No new full-suite/build or browser acceptance is claimed.
- Examined `brand-proposal.ts`, `mcp-profile-fill.ts` and `BrandIntelligenceCard.tsx`. Canonical field integration was unfinished at this earlier checkpoint; the 10 September section above records the subsequent derived-field implementation.
- No PR, migration, deployment, provider call, browser attempt, spend, email or account change in this continuation. Next implementation remains mapped editable brand proposals with lifecycle-safe field provenance, then remaining localization/onboarding review and release sequencing; P2–P5 and R00–R24/D01–D08 remain active.

## Implemented locally

- Scoped accepted/proposed/disputed/expired/rejected records, source references, conflict holding and bounded text/visual selection remain in `project-knowledge.ts`.
- The **unapplied** migration `20260909200000_project_knowledge.sql` now includes service-only private original bytes, SQL/server SHA-256 identity, atomic source/document and source/instruction transactions, scoped original retrieval, exact forget acknowledgement, history, revision checks and project-deletion cleanup. Revocation/replacement/forget remove the previous original; forget also removes derived text/history and leaves late-write tombstones. Limits: 5 MiB/file, 20 MiB current originals/project, 100 sources, 300 records, 10,000 history rows. These are technical bounds, not a commercial allowance.
- `project-knowledge.functions.ts` exposes eleven authenticated operations with server-owned account scope. Review preserves source identity; revert writes a new revision and refuses replaced/revoked source versions. No automatic retry follows an uncertain write. Owner instruction + source now save atomically.
- PDF/DOCX extraction runs in a disposable browser worker, with actual synthetic parser/bundle execution in **Node workers**, not a browser. PDF page references and DOCX paragraph references are retained. Signatures, matching ZIP headers, streamed actual expansion (including dishonest declared sizes), page/text/file/time limits and worker cleanup are covered. JSZip 3.10.2 is now an explicit pinned dependency matching the prior transitive version; PDF.js 6.3.289 and Mammoth 1.12.2 remain pinned. Text-only: no OCR, logo/color/layout interpretation or AI summary is claimed.
- `ProjectKnowledgePanel` extends the existing Brand Intelligence card: recurring owner instructions, editable proposals, review/reject, record revert, source history, current-original text review, replacement, revoke and permanent forget. Conflicting accepted instructions are visibly held. Existing Brand Intelligence form values are not overwritten. Document bytes/text remain component memory and private server storage, never wizard sessionStorage.
- `project-knowledge-website.server.ts` captures an actual bounded homepage excerpt through the existing SSRF-protected fetcher, without a model call. It proposes the captured text; unavailable sources do not replace successful evidence and identical excerpts do not create duplicate proposals. This is limited homepage coverage, not full website/catalog refresh or independent factual verification.
- Onboarding saves the owned project before its first website scan, offers upload/website/skip paths and a review step before foundation/content generation. Pending project identity is reused after interrupted saves; saved services are deduplicated. There is a finish-later path. Raw document material was not added to wizard drafts.
- Ordinary/legacy content and image generation fetch fresh scoped knowledge immediately before provider work. Scheduler and owner benchmark use these shared cores. Exact source/record revisions and fingerprints survive generation archives, normal client/scheduler saves, image attachment and recovery. Image context has a separate 5,500-byte cap within the existing total prompt limit. Unknown knowledge prevents a provider call; no stale-cache fallback exists.

## Verification and remaining P1 work

The final combined suite passed **2,392 tests / 166 files**, including four worker-lifecycle tests. TypeScript and focused lint pass. The final integrated production build passes. Logs use `/tmp/milo-p1-*` (full-tests, types, lint, build, parser-tests, client-tests). The first integrated build caught a `.client` import in server rendering; it was fixed using `createClientOnlyFn` and the subsequent integrated build passed. An earlier full-suite failure came from two existing provider-test fixtures lacking the new lookup; they were updated and coverage added for authenticated retrieval, retained references and no-provider-on-failure behavior.

P1 is **not accepted or released**. Still required:
1. Independently review the 10 September mapped brand summary, shared field-ownership rules, review ergonomics, onboarding and four-locale controls. Wider 24-language delivery remains R20.
2. Final regression/types/build/lint and release review, including migration/backfill/rollback sequencing. Do not deploy the new generation lookup ahead of its migration. Do not alter or reapply the already released P0 migration.
3. Real signed-in upload/edit/revoke/reload and responsive acceptance remain unavailable under the standing browser-policy restriction. Node parser tests and successful builds are not browser acceptance. Do not retry or bypass that restriction.
4. Continue P2 website/catalog changes and dependent-output freshness, then P3 weekly preparation/final holds, P4 real specialist work/faces and scoped lessons, P5 measured outcomes/evaluated workflow improvement. Retain all R00–R24/D01–D08 requirements. No overall completion claim.

## Completed work and authority — do not repeat

P0 recovery PR #107 is merged, migrated once and runtime-verified. See [release evidence](generation-result-recovery-work-in-progress-2026-09-09.md). Main/runtime `17372b78d65ada90a418a336f4473bc125626b41`; deployment `f9fbd9dc-f391-4494-a5f8-b00b359e65f8`; build `1788981897171`; fingerprint `abb0ea3a5308a3817e36a522c185d790e8440ff4c3e3871d808e7ecafe33161c`; exact clean/full/component match at 19:26 UTC. Home/MCP 200/200/204/401. Recovery migration SHA256 `5befeab7b70fc46c3b4a87e3b564c85eab5f927a346ec138a40e3096e31b6ed7`; installed once. Earlier #103/#104/#101/#98 migrations also installed. P0 signed-in recovery/download/visual acceptance remains unverified due to the recorded browser restriction.

No paid AI call, funding, email, client content publication, secret/account or Stripe change occurred. OpenAI remains last recorded UNAUTHORIZED; no key creation/configuration success exists. Do not repeat unchanged authentication or request secrets. Existing personal/Synergy account exception and **USD5 total for exactly one scan, one article and one image, no retries/additional attempts** remain unused. Use only restricted budgets/permits after legitimate secure setup; obey the key-picker immediate-end-turn rule if it succeeds. Zero budgets/permits/native requests/receipts were verified at P0 release. The one operational test email authorization is already consumed; no additional email/outreach/purchases. Stripe is owner-deferred.

Andersen vendor owner/admin/billing default remains `rafi@anderseninnovations.com`; keep client-controlled ownership separate. No secrets in chat/logs/repositories. Browser policy denial must not be bypassed. No subagents without explicit authorization. Public paid launch is not accepted. Preserve open proposal PRs #2/#58/#62; Worker #35/#43 retains its separate boundary. Do not mark the whole plan complete.
