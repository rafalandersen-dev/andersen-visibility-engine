# P1 project knowledge — preserved groundwork

Updated 9 September 2026. Status: **WIP; not wired, reviewed for release, migrated or deployed**. Worktree `/Users/rafi/Projects/milo-growth-project-knowledge-20260909`, branch `codex/milo-project-knowledge-20260909`, based on merged #107 `17372b78d65ada90a418a336f4473bc125626b41`. No P1 PR exists. Full agreed [P1–P5 plan](../product/AGENT_WEEKLY_PLAN_2026_09_09.md) and R00–R24/D01–D08 remain active.

## Current implementation

- `project-knowledge.ts`: strict sourced-record contract; scope/revision/source-fingerprint checks; accepted versus proposed/disputed/expired/rejected states; text/visual/both selection; conflicting accepted values held rather than latest-wins; source revocation/replacement stops future context; exact included references and bounded UTF-8 prompt context. Source-reported facts do not become independently verified facts. Generated articles cannot be independent source kinds.
- `20260909200000_project_knowledge.sql`: proposed service-only source/record/history/tombstone storage. Checks current owned project, serializes with existing workspace lock order, compare-and-set revisions, identical replay, source-version checks, permanent forget with text/history removal and late-save tombstones, scoped history pages. Source kinds and record source identity cannot be silently rewritten. Technical caps: 100 sources, 300 current records and 10,000 history rows per project; these are not billing promises. **Migration is not applied.**
- `project-knowledge.server.ts`: scoped validated RPC helpers, bounded waits, no retry on uncertain writes, fresh context retrieval without a stale cache. Authenticated browser functions have NOT been added; do not expose these helpers without authentication/project checks. Review forget acknowledgement validation and complete history/revert handling while wiring the UI.
- `brand-document.ts`, `.client.ts`, `.worker.ts`: unfinished browser-worker extraction path. Magic-byte/ZIP envelope checks, 5 MiB input, 40 PDF pages, 100,000 text characters, DOCX archive expansion guards, 20-second disposable worker. Plain text only with page/paragraph locators and explicit text-only warning. No document HTML is rendered; the browser DOCX adapter refuses external-file access. Original private document storage, extraction proposals and actual UI are still missing.
- Added pinned `pdfjs-dist@6.3.289` and `mammoth@1.12.2`; package/lock changes are intentional. Independent local node_modules now exists (initial shared symlink was removed before installation). npm audit shows two moderate inherited Vitest/@vitest-mocker development issues, no high/critical or new parser runtime advisories. Do not perform unrelated upgrades. npm reported esbuild/fsevents install scripts not approved; verify actual build behavior before changing script permissions.

## Verification and limits

**40 focused tests / 4 files pass with the repository's Vitest 4.1.10**: selection/scope/revoke/conflicts/context bounds (17), SQL semantics/permissions (9), server boundary (5), document envelope/text limits (9). TypeScript passes; focused lint passes after replacing a null-control regex. Logs `/tmp/milo-knowledge-foundation-tests.log`, `/tmp/milo-knowledge-types.log`, `/tmp/milo-knowledge-lint.log`.

These are foundation checks only. No full P1 suite/build, actual PDF/DOCX fixture extraction, worker bundle execution, authenticated UI or visual acceptance is claimed. One early concurrent dependency/test command briefly used temporary Vitest 5; its result is superseded by the explicit 40-test run on 4.1.10. Use installed dependencies after setup completes.

## Next concrete work

1. Review the foundation and finish real synthetic PDF/DOCX extraction tests, malformed/compressed-file limits and Vite worker bundling. No actual browser automation is allowed through the previously denied administrator-policy path; do not bypass it with another browser/control method. Tests may exercise parsing libraries without that restricted browser surface. Scanned/image-only PDFs need a truthful unsupported/manual route; do not pretend OCR/visual rule extraction happened.
2. Add private original-document storage with server-computed identity/hash, owned-project checks and revoke/forget cleanup. Build authenticated source/record/history/review functions; incomplete/uncertain saves must not overwrite accepted values or silently retry writes. The source RPC currently stores metadata, not original bytes or full extraction segments.
3. Extend existing `BrandIntelligenceCard` and `/app/setup` with What Milo knows / Teach Milo / Forget/revert and reviewable source history; preserve existing form field ownership. Add optional Upload brand guidelines / Build from website / Skip setup. Onboarding saves the project before provider work: attach confirmed knowledge before native generation, never create a competing project profile. Do not persist raw documents in the wizard's unscoped sessionStorage draft.
4. Share authoritative scoped context across ordinary/legacy text, images, scheduler and owner-benchmark paths, and persist the exact source/dependency revisions used by each output. Current generation still uses the old brand brief; the new helpers are not called. Keep approval versions, budgets and client isolation. Website scan AI summaries are suggestions, not independent source evidence; retain actual source text/locators when building source records.
5. Test replacement, revocation, revert, existing owner edits, permissions and unknown outcomes; then full tests/types/build/review and exact migration/runtime release. Continue P2 source refresh, P3 weekly workflow, P4 real specialist team/lessons, P5 measured learning, preserving wider scope.

## Completed work and authority — do not repeat

P0 recovery PR #107 is merged, migrated once and runtime-verified. See [release evidence](generation-result-recovery-work-in-progress-2026-09-09.md). Main/runtime `17372b78d65ada90a418a336f4473bc125626b41`; deployment `f9fbd9dc-f391-4494-a5f8-b00b359e65f8`; build `1788981897171`; fingerprint `abb0ea3a5308a3817e36a522c185d790e8440ff4c3e3871d808e7ecafe33161c`; exact clean/full/component match at 19:26 UTC. Home/MCP 200/200/204/401. Recovery migration SHA256 `5befeab7b70fc46c3b4a87e3b564c85eab5f927a346ec138a40e3096e31b6ed7`; installed once. Earlier #103/#104/#101/#98 migrations also installed. P0 signed-in recovery/download/visual acceptance remains unverified due to the recorded browser restriction.

No paid AI call, funding, email, client content publication, secret/account or Stripe change occurred. OpenAI remains last recorded UNAUTHORIZED; no key creation/configuration success exists. Do not repeat unchanged authentication or request secrets. Existing personal/Synergy account exception and **USD5 total for exactly one scan, one article and one image, no retries/additional attempts** remain unused. Use only restricted budgets/permits after legitimate secure setup; obey the key-picker immediate-end-turn rule if it succeeds. Zero budgets/permits/native requests/receipts were verified at P0 release. The one operational test email authorization is already consumed; no additional email/outreach/purchases. Stripe is owner-deferred.

Andersen vendor owner/admin/billing default remains `rafi@anderseninnovations.com`; keep client-controlled ownership separate. No secrets in chat/logs/repositories. Browser policy denial must not be bypassed. No subagents without explicit authorization. Public paid launch is not accepted. Preserve open proposal PRs #2/#58/#62; Worker #35/#43 retains its separate boundary. Do not mark the whole plan complete.
