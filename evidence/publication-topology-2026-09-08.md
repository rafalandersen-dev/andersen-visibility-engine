# Publication destination discovery — 2026-09-08

Read-only investigation following the owner's WordPress/Lovable architecture hint. No publication retry, queue reset, CMS write, credential change or new database was performed.

## Butelki Wodorowe: destination access found

- Lovable workspace `Si Longevity`, `0YrLHHf6fq9algeLZJY0`; existing collaborator access.
- Project **Butelki Wodorowe website**, `bef4e87a-5b83-44fc-8e98-d3871690d545`.
- Repository `rafalandersen-dev/si-longevity-design-refresh`; GitHub and Lovable both at `095952c3ce5d1fb336febb6f16553578f8a59419` during inspection.
- `supabase/config.toml` maps the project to `ojowadobpddypiotwipq`, exactly the custom connector configured in Milo. The database is enabled and queryable through the existing Lovable connector. No additional Supabase account or access grant is needed for these reads.
- WordPress/WooCommerce provides products, cart handoff and final checkout through `WC_URL` and the existing bridge (`docs/wordpress-hybrid-checkout.md`, `src/lib/woo.server.ts`, `src/lib/cart.server.ts`). Credentials were not read out.
- Milo articles use a separate path: custom `milo-publish` -> `public.milo_content` working copy -> `milo-publish-live` published snapshot -> Lovable frontend `/blog/$slug`. `src/lib/milo.functions.ts` also feeds `/blog`, `/poradnik` and the sitemap. This path does not publish WordPress posts.

### Historical failure is a content-readiness block

Queue `34f82b69-3e29-4bcf-a2ad-ff5734ea0646`, asset `62tomerb`, remains failed with one attempt. Its stored error identifies four unresolved internal links and medical-claim / named-author / human-review requirements. It is not evidence of a failed Supabase connection. No row with this asset ID or its exact slug was found in the destination table at inspection. Do not bypass the review gate or fabricate author/source evidence to retry it.

Destination inventory: six published rows and one draft. Counts alone do not establish editorial correctness. One older draft shares a slug with a published row and retains a historical published_at timestamp; use status and the published snapshot rather than published_at alone.

### Real unattended publication verified

An existing scheduled item (not created or generated by this benchmark) completed at **2026-09-08 07:00 UTC / 09:00 Europe/Stockholm**:

- Asset `bw-art-2026-09-08`, project `butelki-wodorowe`.
- Milo queue: published, attempts=1, published_at `07:00:13.907Z`.
- Destination: matching asset/project, published snapshot, published_at `07:00:13.944Z`.
- Public GET returned success and the expected article heading and self-canonical URL: https://butelkiwodorowe.pl/blog/stezenie-wodoru-w-wodzie-jak-czytac-ppm-i-ppb.
- Heading: "Stężenie wodoru w wodzie: jak czytać ppm i ppb bez marketingowej mgły".

This verifies one scheduled publication through the real destination and public page. It does not prove fresh generation costs, all publishing paths or the complete autopilot plan.

## Andersen UK: correct project mapping found, publishing layer unresolved

- Repository `rafalandersen-dev/scandi-system-studio`, Lovable project `72a54a37-d8e3-4258-ba23-0e58050cfa00`.
- Its current `supabase/config.toml` identifies `umxhrseiwwzjeiqjkcwn`, matching Milo's historical custom endpoint.
- Accessible public schema contains lead/email tables and no `milo_content` table. Current repository tree contains no Milo publishing functions/routes. Do not infer historical deletion or its actor from present absence alone.
- The two historical queue errors remain HTTP502. No retries or migrations were performed; rebuilding the UK publishing destination requires preserving the current site contract and verifying its intended editorial route.

## Superseded access assumptions

Earlier inspection searched only the personal workspace and selected the unrelated **Si Longevity: Hydrogen Bottles** project with no database. All accessible workspaces have now been listed. The client workspace contains the exact Butelki repository/project and the existing access works. The request asking the owner where to find that database is resolved for this investigation.
