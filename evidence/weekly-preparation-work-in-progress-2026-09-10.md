# P3 weekly preparation — groundwork, not deployed

Owner continuation remains active. This isolated checkout `/tmp/milo-p3-weekly-20260910`, branch `codex/milo-p3-weekly-20260910`, starts at P2 candidate `599b286185aef5fe6667ff8153f42b8f74c356d1`. P2 PR #109 is under independent review; do not deploy P3 with P2. No P3 PR or migration deployment exists.

Implemented: `weekly-preparation.ts` computes project-local weeks, configurable previous-week preparation day/time and review lead hours, stable project/instant slot identities, explicit nonexistent-DST and insufficient-review-time gaps, and deterministic earlier repeated-DST instants. Legacy undated monthly drafts reserve against their full original month before intersection with a week; saved/queued dates remain reserved across month boundaries. This is pure planning with no provider, network, email or publishing calls.

Proposed UNAPPLIED `20260910150000_weekly_preparation.sql`: service-RPC-only per-project coordinator state (monthly/weekly/paused) with revision-checked settings. Existing projects default monthly until explicit cutover. The common scheduler lease is widened to weekly periods while enforcing the selected engine in the database, so old monthly code cannot start a paid run after weekly cutover. Switching/pausing/rollback refuses any active or unknown lease, including expired work; original recovery must resolve it. No cron is created and no project is switched by this migration.

**13 tests / 2 files pass**, including actual PGlite migration execution, foreign-project/browser-role rejection, duplicate starts, expiry/recovery, stale settings, cutover/rollback and timezone/calendar cases. TypeScript and focused lint also pass. This is not the weekly executor.

Next: server-authenticated settings and weekly read model, durable slot/stage identities and uncertain-attempt recovery, shared scheduler integration with pre-research source refresh, fixed slot/output IDs, persisted partial results and budget boundaries; exact asset-version approval/holds and durable deduplicated in-app summaries; UI in four locales. Monthly migration must retain existing output IDs, drafts, dates, approvals, pauses and cancellations and keep exactly one scheduler authoritative. No automatic source revision or paid replay may replace owner work. Then tests/review and migration-before-code release. P2 must be integrated/released first; pull any P2 review fixes/merge into this branch without rewriting history. P4/P5 and R00–R24/D01–D08 remain active.

All inherited limits remain: no subagents, no live paid/provider test, no OpenAI reconnect/key setup, no additional emails/outreach/purchases/client publication, no browser-policy bypass, no Codex automation. Existing USD5 one-scan/one-article/one-image scope is unused and funds no weekly production. Continue independent local work when external acceptance is blocked.

## Successor integration progress

P2 correction commit `32cc718` has been merged normally into this separate P3 branch. Authenticated read/update handlers now derive owner identity from middleware; strict scope/settings validation and revision-bound acknowledgements fail closed on malformed/unavailable storage. An uncertain settings response is not automatically retried. **17 tests / 3 files, TypeScript and focused lint pass** (`/tmp/milo-p3-settings-tests.log`, `/tmp/milo-p3-settings-types.log`). This adds settings plumbing only; weekly execution, read-model UI, recovery, version approval and notification integration remain unfinished. No database application, automatic engine switch or provider call.

Durable stage foundation added to the still-unapplied P3 migration: unique project/publication-instant/stage records, server-allocated request/output IDs, immutable input hash, no new paid attempt after unknown outcome, bounded research/result acknowledgements and project deletion cascade. Shared-lease restart and monthly cutover refuse unresolved stages. The configured project timezone must place a stage in its owned weekly period. Content/image completion requires the exact archived receipt/native attempt/output/project; read-only recovery can reconcile an archive after a lost final acknowledgement without calling a provider. A server orchestration helper returns retained results, holds uncertain/mismatched claims and never retries work after acknowledgement failure. **26 tests / 4 files, TypeScript and focused lint pass** (`/tmp/milo-p3-stages-all-tests.log`, `/tmp/milo-p3-stages-types.log`). Executor/read-model/UI, explicit cancellation/recovery controls, exact-version approval and deduplicated in-app summary remain next. No P3 PR or migration application.

Weekly readiness now has an authenticated read model using project-scoped real queue rows; it distinguishes drafted/queued/publishing/published/held/cancelled, flags competing slot claims and keeps legacy reservations. Cancelled/failed queue dates remain reserved; approved assets or browser schedule mirrors alone never become queued. Reads fail closed on missing scope, malformed queue or over 1,000 rows. **Five readiness tests / two files, TypeScript and focused lint pass** (`/tmp/milo-p3-readiness-tests.log`, `/tmp/milo-p3-readiness-types.log`). These add to the 26 planner/settings/stage tests. No visible UI or weekly executor is wired yet. P2 corrections through `c2f72a8` are merged into this branch; do not deploy until P2 release and P3 completion/review.

The stage helper now automatically attempts database-only reconciliation for an unknown content/image stage before returning a recovery hold; it never reruns the generation callback. A matching archived receipt returns through the same strict result parser. Research uncertainty remains held because it has no equivalent independent archive. Six stage-helper tests, TypeScript and lint pass (`/tmp/milo-p3-reconcile-tests.log`, `/tmp/milo-p3-reconcile-types.log`). P2 held-date correction `dcc54d5` is merged normally. No executor/UI activation or provider call.

Stage admission also rechecks the authoritative project's enabled flag inside the database lock, so an owner pause takes effect before another stage starts even if a previously claimed lease still exists. Configured timezone whitespace is normalized consistently with the shared planner. Ten actual migration tests, types and focused lint pass (`/tmp/milo-p3-stage-pause-tests.log`, `/tmp/milo-p3-stage-pause-types.log`). Still no executor/provider/UI activation.
