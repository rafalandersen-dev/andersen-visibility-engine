# Output knowledge revalidation — 11 September 2026

Status: implemented locally; independent review, Linux validation and release gates pending. No new SQL applied or deployment started. Prior PR121 and all earlier releases remain installed and must not be repeated.

Owners can inspect the saved assembled deliverable and original/current knowledge versions, explicitly acknowledge each current fact and record a version-bound revalidation. Saved text and original provenance are unchanged. Publication rechecks the actual saved candidate, current evidence context, accepted/applicable knowledge, expiry and exact active review history. Forgotten evidence is never waived; website-source and separate publication-approval gates remain.

Project Setup supports the workflow in English, Polish, Swedish and Danish. History and exact withdrawal remain accessible after a current hold clears and load independently of a failed content preview. The affected-draft list uses one bounded atomic project read for up to 100 assets, checks future publication expiry, and reports any uninspected remainder.

Unapplied migration20260911010000_output_knowledge_reviews.sql adds one private metadata-only history table, five service-only RPCs, two trigger-only functions and four invalidation triggers. Workspace/evidence hashes and account locking prevent stale saves; identical acknowledgements are idempotent; withdrawn IDs cannot revive. Relevant edits and knowledge changes withdraw authority, but separate approval/schedule timestamps alone preserve it. Project/output deletion clears history. No facts, documents or output text are copied into history. Existing released migrations and schedules are unchanged.

Full local suite: 3,082 tests / 227 files pass, including single-session PGlite fixtures. TypeScript, focused lint and production build also passed after the final independent-history UI adjustment. Initial full run failed only an outdated endpoint-count assertion (11 vs 15); its replacement checks all exports and exact authenticated-owner binding. A new forged-owner assertion initially expected an asynchronous rejection from a synchronous test validator; it was corrected without changing production validation, and the full suite rerun passed. An earlier combined SQL fixture setup timed out under concurrent checks; isolated SQL rerun passed.

No signed-in/browser, live-provider/CMS/Google/supplier or multi-session database acceptance is claimed. No funding/generation, messages/outreach/client publication, credentials/accounts/MFA changes, browser-policy retries, subagents or new automation. Full R00–R24/D01–D08 and separate PR2/58/62/Worker35/43 boundaries persist. Overall progress remains about55%, technical implementation about70%; public paid launch remains NO-GO.

Initial exact code reviewb96e20d completed06:07:22UTC. Finding3986371795 correctly identified source-dependency augmentation contaminating the publication-version comparison. The source gate now preserves the original input for both knowledge checks and the final recheck, while independently refreshing and validating the registry-augmented source dependencies. A regression verifies both paths receive the exact original object and retained source evidence still refreshes. Updated65tests/4files, types, focusedlint and build pass. InitialLinux34568423881 passed3,082tests/227files on bothBunversions, types/build/frozenlocks. A new exact-head review/Linux result is required for the fix.

Fresh read-only baseline on11September: usage11rows/227units (previous release10/226), private/native/knowledge/registry/identity rows0; queue101review_required/31published/5failed; monthly/weekly schedules and command hashes unchanged. No cause is inferred for the intervening usage change; compare the actual fresh baseline around release. No SQL/deployment/provider action was performed.
