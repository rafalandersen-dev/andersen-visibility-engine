# Shared scheduler preparation capacity

R06 requires visible quota shortages and interrupted-work explanations. The monthly scheduler already caps generation from the server-owned usage counter, but the notification sweep did not warn when remaining capacity could not cover the enabled schedule.

This packet projects the same next-month horizon, configured weekdays/time zone, booked slots and saved/legacy held-draft accounting as the existing scheduler. It sums missing preparation across all active projects in the owner's workspace before comparing against the shared account counter. It does not allocate a separate copy of the account allowance to every project. Disabled schedules and fully covered projects produce no capacity event.

The server reads only plan/status/period-end, the owner role, and current UTC-month contentGeneration usage, with account filters and a two-row bound to detect unexpected duplicates. Missing rows have their existing free/no-use meaning. Errors, malformed values and duplicates yield unavailable evidence, not a zero allowance. Existing entitlement/cap semantics are reused. These separate reads are a planning snapshot, not a transaction or reservation; execution still performs atomic claims. Current counters measure preparation attempts, not confirmed delivered-result allowances or monetary budgets. The UI states the current usage period and next planning period explicitly; it does not promise that attempts produce completed articles.

Two incident kinds distinguish low capacity from unavailable verification. Their keys are stable per project, kind and usage/planning period; changed counts update the current incident. A fresh scan resolves shortage after additional capacity, fewer slots, saved work or pause. Metering failures keep the other operational events available. No quotas, funded budgets, model calls, subscriptions, automatic retries, approvals or publication permissions are changed. The existing opt-in outbox can render the new kinds in PL/EN/SV/DA, but this packet neither enables email nor sends anything.

Migration234500 only adds the two accepted kinds to the existing table constraint and server-only sync function. Its exact SQL passed a live catalog/privilege check inside a rolled-back transaction. Follow-up confirmed the original constraint, no migration registration and all13 projects retained. No persistent migration or notification fixture was applied during this check. Apply/register only after review.

Tests cover aggregate demand, existing and legacy drafts without double counting, project isolation, disabled/full schedules, year rollover, stable incident IDs, unknown-vs-zero data, strict account/period/bucket reads, owner caps, expired plans, bounded/malformed/error responses, coexistence with other alerts, four-locale digest rendering and PostgreSQL deduplication/resolution. The acceptance logs and final combined counts are recorded in the PR. No real AI workload, recipient email or customer article was used.

Combined with #86 main and the latest #87 recipient-verification source, 1,657 tests in125 files, TypeScript, production build, focused lint and diff checks pass. The final notification schema also refuses absent or contradictory capacity details rather than rendering missing evidence as zero. Migration234500 remains UNAPPLIED pending review.

Completion: #88 head1264bbff014906cc6a81a5cef70c61cff0fc64b9 passed final review without comments and merged as00d2a6f6d0728f06ce9a8a3012c9bbf14f5cc55f. The exact migration234500 was applied and registered atomically at approximately21:14UTC; accepted kinds and server-only sync privileges verified, all13 projects retained and email outbox0. Do not reapply. Deployment8149d4cf-3986-4d02-ac66-4e3647d77279 produced build1788815686498, exact revision, modified=false, fingerprinte090c4957e8fdc8d4182bbee9ea3cfc2433c39560de3efe85dfe99363d05a82e and all components matching the clean main checkout. This delivers the capacity notification slice; it does not complete monetary accounting or all R05/R06 scope.

Background observation: cron150 remains active every15minutes. Its21:15 dispatch succeeded, and the application persisted a completed scan at21:15:01UTC. Five account scan records and four active inbox events were present; email opt-ins0/outbox0. This is evidence of a completed server sweep, not a paid generation run or email delivery.
