# Citation preparation review — 19 September 2026

Candidate PR137 starts at 9a3e564ee02b8416dd3a3d2d737ce7dd1fd4a680, stacked on PR136. Its PR description is stale: the actual tree contains native report, panel and finding preparation modules and tests, not documentation alone. No parser, storage endpoint, production deployment or real client evidence is present.

Independent counterexamples against the unchanged candidate (Node executing its actual pure modules, synthetic records only) reproduced:

- A complete native snapshot containing one reviewed zero and a second row with the metric missing returns observed:false. Missing evidence must remain unknown.
- URL-prefix properties /Shop and /shop share a scope key despite case-sensitive paths.
- A raw cell containing a numeric prefix followed by oversized invalid material becomes known_value after truncation.
- Two copies of one verified improvement count as two improvements.

Frozen reproduction: /tmp/milo-citation-counterexamples-20260919/probe.mjs and before.log. Additional source review found incomplete panel/session identity checks, duplicate/out-of-panel count inflation and invalid retest chronology. Claude is implementing bounded pure-module corrections and regression tests. Validation is pending, not acceptance.

Boundary: D03's accepted v1 spec remains native Google/Bing evidence, one manual consumer surface, human-reviewed findings, two distinct verified improvements and one comparable retest. Genuine report exports and owner-approved panel/session controls are still required. Do not invent export columns, observed zeros, evidence receipts or four-week pilot results. No provider requests, SQL, billing, collection automation, owner approvals or publication occur in this worktree.

First correction checkpoint: 42 focused tests / 3 files pass. Independent rerun shows missing metric => unknown, /Shop vs /shop => distinct, oversized numeric-prefix input => invalid, duplicate improvement => count one. Further independent probe still finds comparablePairs accepts round 9 on a four-round panel and silently chooses a contradictory duplicate follow-up; draft/unapproved protocol also produces no deviation. These remaining findings are queued in /tmp/milo-citation-review-followup.txt. Candidate is not ready to commit or accept. Temporary Claude runtests.sh artifact was inspected and removed; no production action.

Scope audit found LAUNCH_READINESS.md still enforcing the superseded three-surface floor in G7 and its public-release checklist, outside the current scope-guard file list. Both active gate statements are now reconciled to the accepted 14 September D03 specification, preserving the historical decision as superseded. Scope-guard coverage of this canonical gate file is queued with the remaining bounded review followup. No public-release GO is granted.

Second review corrections were completed locally by Codex after the independent counterexamples: shared slot validation now protects both counting and pairing, invalid comparison rounds refuse acceptance, and unapproved/draft panels cannot establish comparable captures. Planned-slot preview remains available; brand diagnostics cannot bypass the discovery retest gate. LAUNCH_READINESS.md is included in the scope guard. All 51 focused tests / 3 files and TypeScript pass. Independent final bundled-module probe returns comparable:false for both out-of-range and contradictory duplicate captures, and panel_not_approved for the draft. No need to execute the superseded /tmp/milo-citation-review-followup.txt; its findings are addressed locally. Full integrated validation/release remains pending.

Integrated checkpoint: local merge d87e6a37 incorporates PR136 head 5e9baf532fe89de223f51c9dd9c258eb9bb3f633. Both merge conflicts were documentation-only; the USD50/manual-free budget decision, owner payer and the accepted D03 scope were retained. Full integrated suite passes 5,980 tests / 375 files with --maxWorkers=2 (64.17 seconds), followed sequentially by a passing production build. Integrated TypeScript and changed-file lint/format pass. This is candidate validation, not migration, deployment or real-export acceptance.

External-review corrections (bounded, pure preparation modules; no parser, storage, endpoint, provider, migration or export acceptance added). Five findings addressed with regression tests; verification is left to Codex and is not asserted here.

1. Native import boundary (`native-ai-report.ts`): `nativeCellSchema` now requires a stored/imported cell to agree with the deterministic interpretation of its own `raw`. A `known_value`/`preliminary` cell must equal `interpretExportCell(raw, unit)` exactly (applying the numeric-bounds, formula-rejection and export-zero rules), so a value unrelated to the raw text cannot enter as positive evidence; a reviewed `known_zero` must sit on a raw that reads as a numeric zero; unknown/invalid cells stay no-positive-evidence and an oversized cell truncated to a numeric prefix stays `invalid`. Added forged raw/value/status cases and genuine `resolveExportZero` round-trip cases.
2. Slot binding (`citation-panel.ts`): `slotOutcome` binds the answer's `promptId` and `promptRevision` to the panel slot's versioned question and no longer accepts status alone; call sites and fixtures updated; wrong prompt/version tested.
3. Methodology lock (`citation-panel.ts`): the locked `surfaceSchema` now carries the expected `modelLabel` and `webSearchEvidenced`, and `protocolDeviations` breaks on a known model-label change or a differing/unknown-where-locked web-search-evidence state, preserving unknown as unknown. Baseline/follow-up share the locked methodology transitively because only `complete` slots are eligible. Tests added.
4. Verified-improvement evidence (`citation-finding.ts`): a verified improvement now requires non-empty `baselineCaptureIds` in both the schema refinement and `isVerifiedImprovement`; unverified drafts may omit them. Tested.
5. Retest gate (`citation-panel.ts`, `citation-finding.ts`): `comparablePairs` now takes explicit scoped evidence (captures with identity + panel/client, findings with panel/client) and resolves each improvement's `findingIds`/`baselineCaptureIds` against those records, rejecting cross-panel/client, missing and duplicate-alias references. The CI-3 threshold counts *distinct substantive* changes (task, or destination + approved version) via `countDistinctSubstantiveChanges`, so a clone with a fresh UUID cannot bypass the two-change gate, and each change must be verified after its actual baseline and before the follow-up. Tests prove cross-panel/client rejection, cloned-change rejection and valid two-distinct-change acceptance.

## Stage validation attempt — Claude, 19 September 2026 (test/build execution blocked in this session)

Under the 19 September working agreement (Claude tests/docs, Codex independent review/release), this session set out to validate the committed five-fix stage (`09206935`, integrated with PR136 `cba00f8` in merge `406cc872`). Working tree clean at start.

Distinguish clearly: the focused **51 tests / 3 files**, the integrated **5,980 tests / 375 files (64.17s)** and the passing production build recorded higher in this file were run in **earlier** Codex/integration sessions. They are **not** re-run or re-claimed here, and they predate the two-file layout below.

**What was executed and verified this session (real results):**

- `git status --short` — clean working tree; `git log` confirms HEAD `406cc872`, five-fix commit `09206935`.
- `git diff --check 09206935~1 09206935` — **clean** (no whitespace/conflict-marker errors across the five-fix range).
- Static source review of all five fixes (files re-read in full): the `nativeCellSchema` refinement compares against `interpretExportCell(raw, unit)` (no `preliminary` flag), so a genuine preliminary cell still matches on the numeric `known_value` interpretation and a reviewed `known_zero` must sit on an `unknown_export_zero` raw; `slotOutcome`, `protocolDeviations`, `isVerifiedImprovement`, `substantiveChangeKeys`/`countDistinctSubstantiveChanges` and `comparablePairs` are internally coherent with the docs. **No new defect found; no code change made** (only documentation was edited — see below).
- Static test inventory (structural, `grep`, **not** a runtime pass count): `native-ai-report.test.ts` defines 15 and `citation-panel.test.ts` defines 26 literal `it`/`test` blocks (41 total; `citation-panel.test.ts` contains one `.each` that expands to more at runtime). There is **no** separate `citation-finding.test.ts`; the finding tests are folded into `citation-panel.test.ts` (the "if finding tests integrated into panel, use existing suites" case).

**What could NOT be executed this session (no counts fabricated):** the focused `vitest run` of the two suites, the full `vitest run --maxWorkers=2`, `tsc --noEmit`, scoped `eslint`, scoped `prettier --check`, and `vite build`. Reason: the sandboxed shell cannot spawn (`E2BIG` — the sandbox wrapper's env exceeds the kernel `ARG_MAX`, reproduced on every sandboxed command), and with the sandbox disabled only a small read allowlist (`git`, `node --version`) is admitted — every code-executing toolchain invocation (`vitest`, `tsc`, `eslint`, `prettier`, `vite build`, even `node -e`) was rejected at dispatch with "Run outside of the sandbox". No shell wrapper was written and no forbidden path (git mutation, network, provider, DB, email, deployment, dependency install, credentials, memory, other worktree) was attempted. The owner can lift this with `/sandbox` or by granting per-command approval for the read-only toolchain runs, exactly as the earlier sessions recorded above did; the same commands are reproducible. **These suites therefore remain unverified in this session** — the earlier recorded pass counts stand as Codex's evidence, not as a re-validation here.

**Trust-boundary documentation (docs ownership; the only file changed this session).** `docs/CITATION-INTELLIGENCE-PANEL-AND-REVIEW.md` gains an explicit trust-boundary note on `comparablePairs`: it independently derives **only** identity/scope binding (each capture and finding must resolve to a record for this exact panel id, version and client), reference uniqueness, the verified-improvement chronology (verification after every baseline it improves; follow-up strictly after its baseline and after both distinct changes) and the distinct-substantive-change count. It does **not** re-derive a capture's `outcome`: `complete` is a **trusted upstream status** from `slotOutcome`/`protocolDeviations` over the manual answer record, and `citationsComplete`/`ownCitation` are trusted human-reviewed facts. The helper operates on already-parsed in-memory records; it is **not** an endpoint or storage parser and does not by itself validate arbitrary/untrusted client input, and no such parser exists in this preparation PR. The proof loop is not circular: an improvement's verification (destination receipt + approved version + scoped finding and baseline) is established **before** the retest, and the comparable follow-up must be captured strictly afterwards, so verification never depends on the retest it enables. This documents existing code behavior (verified by reading the source); it changes no code and asserts no production or real-export validation.

## Stage validation attempt — Claude, 19 September 2026 (exact literal toolchain, spawn-blocked again)

This session retried the outstanding validation with the **exact literal** allowlisted commands (no `./`, no `npx`/`node` wrappers, no pipes/redirects/shell probes, each run separately, no chaining), addressing the prior mismatch where `npx`/`node`/`./` prefixes did not match the literal `node_modules/.bin/...` allowlist.

**Actually attempted this session (real results, no fabricated counts):**

- `node_modules/.bin/vitest run src/lib/native-ai-report.test.ts src/lib/citation-panel.test.ts` → **`E2BIG: argument list too long, posix_spawn '/bin/zsh'`**. No test run; no pass/fail count produced.
- `node_modules/.bin/tsc --noEmit` → **`E2BIG: argument list too long, posix_spawn '/bin/zsh'`**. No type-check produced.

Both failures occur at `posix_spawn '/bin/zsh'`, before the tool itself runs. The short `tsc --noEmit` argument list fails identically to the long two-file `vitest` list, so the cause is **not** the command's own arguments but the wrapper environment passed to the shell exceeding the kernel `ARG_MAX` — the same systemic spawn blocker recorded in the previous attempt above.

**Not attempted (would reproduce the identical spawn failure; stopped shell retries per instruction):** scoped `node_modules/.bin/eslint`, scoped `node_modules/.bin/prettier --check`, `node_modules/.bin/vitest run --maxWorkers=2`, and `node_modules/.bin/vite build`. Each spawns through the same `/bin/zsh`; re-issuing them would be a retry loop of the same environment failure, not new validation. No workaround was taken: no wrapper script, no `npx`/`node`/`./` prefix, no pipe/redirect/env edit, no other-worktree, git-mutation, provider, network, DB or credential action.

**Net result:** the five focused/integrated toolchain checks remain **unverified in this session** — the spawn environment blocks them regardless of matching the literal allowlist. No application code was changed (no change since the five-fix commit `09206935` + integrated merge `406cc872`); this session made no code edits and only recorded this real result. The owner can lift the `E2BIG` spawn condition (e.g. via `/sandbox` or an environment with a smaller wrapper env) and the same exact literal commands are reproducible.

## Final integrated checks (Codex integration exception, 19 September)

Claude's exact-command validation stopped before execution with sandbox-wrapper E2BIG. Codex therefore executed the already-prepared checks in the available task environment without changing security settings or replacing Claude's implementation. Current integrated source (five corrections09206935 + base406cc872): **6006 tests /375 files passed** in58.74s; tsc --noEmit passed; production Vite/Nitro build passed. Scoped ESLint/Prettier and whitespace checks pass after formatter-only normalization of citation-panel.ts and citation-panel.test.ts (nine formatting diagnostics, no semantic change). Logs: /tmp/milo-pr137-final-tests-20260919.log, /tmp/milo-pr137-final-types-20260919.log, /tmp/milo-pr137-final-build-20260919.log. No full-suite rerun after formatting-only normalization. The trust-boundary clarification is documented; actual persistence/import parsing remains required in the later implementation. These are local integration results, not real exports, live citation collection or pilot acceptance.
