# Citation preparation review — 19 September 2026

Candidate PR137 starts at 9a3e564ee02b8416dd3a3d2d737ce7dd1fd4a680, stacked on PR136. Its PR description is stale: the actual tree contains native report, panel and finding preparation modules and tests, not documentation alone. No parser, storage endpoint, production deployment or real client evidence is present.

Independent counterexamples against the unchanged candidate (Node executing its actual pure modules, synthetic records only) reproduced:

- A complete native snapshot containing one reviewed zero and a second row with the metric missing returns observed:false. Missing evidence must remain unknown.
- URL-prefix properties /Shop and /shop share a scope key despite case-sensitive paths.
- A raw cell containing a numeric prefix followed by oversized invalid material becomes known_value after truncation.
- Two copies of one verified improvement count as two improvements.

Frozen reproduction: /tmp/milo-citation-counterexamples-20260919/probe.mjs and before.log. Additional source review found incomplete panel/session identity checks, duplicate/out-of-panel count inflation and invalid retest chronology. Claude is implementing bounded pure-module corrections and regression tests. Validation is pending, not acceptance.

Boundary: D03's accepted v1 spec remains native Google/Bing evidence, one manual consumer surface, human-reviewed findings, two distinct verified improvements and one comparable retest. Genuine report exports and owner-approved panel/session controls are still required. Do not invent export columns, observed zeros, evidence receipts or four-week pilot results. No provider requests, SQL, billing, collection automation, owner approvals or publication occur in this worktree.

First correction checkpoint: 42 focused tests / 3 files pass. Independent rerun shows missing metric => unknown, /Shop vs /shop => distinct, oversized numeric-prefix input => invalid, duplicate improvement => count one. Further independent probe still finds comparablePairs accepts round 9 on a four-round panel and silently chooses a contradictory duplicate follow-up; draft/unapproved protocol also produces no deviation. These remaining findings are queued in /tmp/milo-citation-review-followup.txt. Candidate is not ready to commit or accept. Temporary Claude runtests.sh artifact was inspected and removed; no production action.

Scope audit found LAUNCH_READINESS.md still enforcing the superseded three-surface floor in G7 and its public-release checklist, outside the current scope-guard file list. Both active gate statements are now reconciled to the accepted 14 September D03 specification, preserving the historical decision as superseded. Scope-guard coverage of this canonical gate file is queued with the remaining bounded review followup. No public-release GO is granted.

Second review corrections were completed locally by Codex after the independent counterexamples: shared slot validation now protects both counting and pairing, invalid comparison rounds refuse acceptance, and unapproved/draft panels cannot establish comparable captures. Planned-slot preview remains available; brand diagnostics cannot bypass the discovery retest gate. LAUNCH_READINESS.md is included in the scope guard. All 51 focused tests / 3 files and TypeScript pass. Independent final bundled-module probe returns comparable:false for both out-of-range and contradictory duplicate captures, and panel_not_approved for the draft. No need to execute the superseded /tmp/milo-citation-review-followup.txt; its findings are addressed locally. Full integrated validation/release remains pending.

Integrated checkpoint: local merge d87e6a37 incorporates PR136 head 5e9baf532fe89de223f51c9dd9c258eb9bb3f633. Both merge conflicts were documentation-only; the USD50/manual-free budget decision, owner payer and the accepted D03 scope were retained. Full integrated suite passes 5,980 tests / 375 files with --maxWorkers=2 (64.17 seconds), followed sequentially by a passing production build. Integrated TypeScript and changed-file lint/format pass. This is candidate validation, not migration, deployment or real-export acceptance.

External-review corrections (bounded, pure preparation modules; no parser, storage, endpoint, provider, migration or export acceptance added). Five findings addressed with regression tests; verification is left to Codex and is not asserted here.

1. Native import boundary (`native-ai-report.ts`): `nativeCellSchema` now requires a stored/imported cell to agree with the deterministic interpretation of its own `raw`. A `known_value`/`preliminary` cell must equal `interpretExportCell(raw, unit)` exactly (applying the numeric-bounds, formula-rejection and export-zero rules), so a value unrelated to the raw text cannot enter as positive evidence; a reviewed `known_zero` must sit on a raw that reads as a numeric zero; unknown/invalid cells stay no-positive-evidence and an oversized cell truncated to a numeric prefix stays `invalid`. Added forged raw/value/status cases and genuine `resolveExportZero` round-trip cases.
2. Slot binding (`citation-panel.ts`): `slotOutcome` binds the answer's `promptId` and `promptRevision` to the panel slot's versioned question and no longer accepts status alone; call sites and fixtures updated; wrong prompt/version tested.
3. Methodology lock (`citation-panel.ts`): the locked `surfaceSchema` now carries the expected `modelLabel` and `webSearchEvidenced`, and `protocolDeviations` breaks on a known model-label change or a differing/unknown-where-locked web-search-evidence state, preserving unknown as unknown. Baseline/follow-up share the locked methodology transitively because only `complete` slots are eligible. Tests added.
4. Verified-improvement evidence (`citation-finding.ts`): a verified improvement now requires non-empty `baselineCaptureIds` in both the schema refinement and `isVerifiedImprovement`; unverified drafts may omit them. Tested.
5. Retest gate (`citation-panel.ts`, `citation-finding.ts`): `comparablePairs` now takes explicit scoped evidence (captures with identity + panel/client, findings with panel/client) and resolves each improvement's `findingIds`/`baselineCaptureIds` against those records, rejecting cross-panel/client, missing and duplicate-alias references. The CI-3 threshold counts *distinct substantive* changes (task, or destination + approved version) via `countDistinctSubstantiveChanges`, so a clone with a fresh UUID cannot bypass the two-change gate, and each change must be verified after its actual baseline and before the follow-up. Tests prove cross-panel/client rejection, cloned-change rejection and valid two-distinct-change acceptance.

## Stage validation attempt — Claude, 19 September 2026 (test/build execution blocked in this session)

Under the 19 September working agreement (Claude tests/docs, Codex independent review/release), this session set out to validate the committed five-fix stage (`09206935`, integrated with PR136 `cba00f8` in merge `406cc872`). Working tree clean at start.

Distinguish clearly: the focused **51 tests / 3 files**, the integrated **5,980 tests / 375 files (64.17s)** and the passing production build recorded higher in this file were run in **earlier** Codex/integration sessions. They are **not** re-run or re-claimed here, and they predate the two-file layout below.

**What was executed and verified this session (real results):**

- `git status --short` — clean working tree; `git log` confirms HEAD `406cc872`, five-fix commit `09206935`.
- `git diff --check 09206935~1 09206935` — **clean** (no whitespace/conflict-marker errors across the five-fix range).
- Static source review of all five fixes (files re-read in full): the `nativeCellSchema` refinement compares against `interpretExportCell(raw, unit)` (no `preliminary` flag), so a genuine preliminary cell still matches on the numeric `known_value` interpretation and a reviewed `known_zero` must sit on an `unknown_export_zero` raw; `slotOutcome`, `protocolDeviations`, `isVerifiedImprovement`, `substantiveChangeKeys`/`countDistinctSubstantiveChanges` and `comparablePairs` are internally coherent with the docs. **No new defect found; no code change made** (only documentation was edited — see below).
- Static test inventory (structural, `grep`, **not** a runtime pass count): `native-ai-report.test.ts` defines 15 and `citation-panel.test.ts` defines 26 literal `it`/`test` blocks (41 total; `citation-panel.test.ts` contains one `.each` that expands to more at runtime). There is **no** separate `citation-finding.test.ts`; the finding tests are folded into `citation-panel.test.ts` (the "if finding tests integrated into panel, use existing suites" case).

**What could NOT be executed this session (no counts fabricated):** the focused `vitest run` of the two suites, the full `vitest run --maxWorkers=2`, `tsc --noEmit`, scoped `eslint`, scoped `prettier --check`, and `vite build`. Reason: the sandboxed shell cannot spawn (`E2BIG` — the sandbox wrapper's env exceeds the kernel `ARG_MAX`, reproduced on every sandboxed command), and with the sandbox disabled only a small read allowlist (`git`, `node --version`) is admitted — every code-executing toolchain invocation (`vitest`, `tsc`, `eslint`, `prettier`, `vite build`, even `node -e`) was rejected at dispatch with "Run outside of the sandbox". No shell wrapper was written and no forbidden path (git mutation, network, provider, DB, email, deployment, dependency install, credentials, memory, other worktree) was attempted. The owner can lift this with `/sandbox` or by granting per-command approval for the read-only toolchain runs, exactly as the earlier sessions recorded above did; the same commands are reproducible. **These suites therefore remain unverified in this session** — the earlier recorded pass counts stand as Codex's evidence, not as a re-validation here.

**Trust-boundary documentation (docs ownership; the only file changed this session).** `docs/CITATION-INTELLIGENCE-PANEL-AND-REVIEW.md` gains an explicit trust-boundary note on `comparablePairs`: it independently derives **only** identity/scope binding (each capture and finding must resolve to a record for this exact panel id, version and client), reference uniqueness, the verified-improvement chronology (verification after every baseline it improves; follow-up strictly after its baseline and after both distinct changes) and the distinct-substantive-change count. It does **not** re-derive a capture's `outcome`: `complete` is a **trusted upstream status** from `slotOutcome`/`protocolDeviations` over the manual answer record, and `citationsComplete`/`ownCitation` are trusted human-reviewed facts. The helper operates on already-parsed in-memory records; it is **not** an endpoint or storage parser and does not by itself validate arbitrary/untrusted client input, and no such parser exists in this preparation PR. The proof loop is not circular: an improvement's verification (destination receipt + approved version + scoped finding and baseline) is established **before** the retest, and the comparable follow-up must be captured strictly afterwards, so verification never depends on the retest it enables. This documents existing code behavior (verified by reading the source); it changes no code and asserts no production or real-export validation.

## Stage validation attempt — Claude, 19 September 2026 (exact literal toolchain, spawn-blocked again)

This session retried the outstanding validation with the **exact literal** allowlisted commands (no `./`, no `npx`/`node` wrappers, no pipes/redirects/shell probes, each run separately, no chaining), addressing the prior mismatch where `npx`/`node`/`./` prefixes did not match the literal `node_modules/.bin/...` allowlist.

**Actually attempted this session (real results, no fabricated counts):**

- `node_modules/.bin/vitest run src/lib/native-ai-report.test.ts src/lib/citation-panel.test.ts` → **`E2BIG: argument list too long, posix_spawn '/bin/zsh'`**. No test run; no pass/fail count produced.
- `node_modules/.bin/tsc --noEmit` → **`E2BIG: argument list too long, posix_spawn '/bin/zsh'`**. No type-check produced.

Both failures occur at `posix_spawn '/bin/zsh'`, before the tool itself runs. The short `tsc --noEmit` argument list fails identically to the long two-file `vitest` list, so the cause is **not** the command's own arguments but the wrapper environment passed to the shell exceeding the kernel `ARG_MAX` — the same systemic spawn blocker recorded in the previous attempt above.

**Not attempted (would reproduce the identical spawn failure; stopped shell retries per instruction):** scoped `node_modules/.bin/eslint`, scoped `node_modules/.bin/prettier --check`, `node_modules/.bin/vitest run --maxWorkers=2`, and `node_modules/.bin/vite build`. Each spawns through the same `/bin/zsh`; re-issuing them would be a retry loop of the same environment failure, not new validation. No workaround was taken: no wrapper script, no `npx`/`node`/`./` prefix, no pipe/redirect/env edit, no other-worktree, git-mutation, provider, network, DB or credential action.

**Net result:** the five focused/integrated toolchain checks remain **unverified in this session** — the spawn environment blocks them regardless of matching the literal allowlist. No application code was changed (no change since the five-fix commit `09206935` + integrated merge `406cc872`); this session made no code edits and only recorded this real result. The owner can lift the `E2BIG` spawn condition (e.g. via `/sandbox` or an environment with a smaller wrapper env) and the same exact literal commands are reproducible.

## Final integrated checks (Codex integration exception, 19 September)

Claude's exact-command validation stopped before execution with sandbox-wrapper E2BIG. Codex therefore executed the already-prepared checks in the available task environment without changing security settings or replacing Claude's implementation. Current integrated source (five corrections09206935 + base406cc872): **6006 tests /375 files passed** in58.74s; tsc --noEmit passed; production Vite/Nitro build passed. Scoped ESLint/Prettier and whitespace checks pass after formatter-only normalization of citation-panel.ts and citation-panel.test.ts (nine formatting diagnostics, no semantic change). Logs: /tmp/milo-pr137-final-tests-20260919.log, /tmp/milo-pr137-final-types-20260919.log, /tmp/milo-pr137-final-build-20260919.log. No full-suite rerun after formatting-only normalization. The trust-boundary clarification is documented; actual persistence/import parsing remains required in the later implementation. These are local integration results, not real exports, live citation collection or pilot acceptance.

## Second external-review packet — five new findings (Claude, 19 September; prepared, NOT RUN here)

Bounded pure-module corrections for the five concrete findings raised in the 15:24 fresh review, on top of the integrated five-fix stage. No parser, endpoint, storage, provider, migration, network, DB, deploy or credential work was added; helpers remain preparation only; new public import surfaces stay fail-closed. No git mutation and no other-worktree write. The USD50/manual-free-account budget and accepted D03 scope are unchanged. Owned files only: `citation-finding.ts`, `citation-panel.ts`, `native-ai-report.ts` and their tests/docs. **The CLI E2BIG spawn blocker persists in this session, so the suites below were NOT executed here; Codex runs them under the recorded exception.**

Files changed: `src/lib/citation-finding.ts`, `src/lib/citation-panel.ts`, `src/lib/native-ai-report.ts`, `src/lib/citation-panel.test.ts`, `src/lib/native-ai-report.test.ts`, `docs/CITATION-INTELLIGENCE-PANEL-AND-REVIEW.md`, `docs/CITATION-INTELLIGENCE-NATIVE-IMPORT.md`.

1. **Destination identity (4053596293, P1) — `citation-finding.ts`.** `substantiveChangeKeys` no longer serializes `destination.reference` verbatim. A new private `canonicalDestinationReference` canonicalizes a `public_url` by standard URL resource identity via the `URL` parser: scheme + host case-folded and default port dropped by the parser, fragment discarded (never sent to the server), path and query kept exact and case-sensitive (spec §8 "do not silently collapse meaningful query/path differences"). An invalid or non-http(s) reference **fails closed** (throws), so it can neither collide with nor masquerade as distinct from another destination. `listing`/`configuration` are opaque contract identifiers compared exactly, never URL-folded. Regressions in `citation-panel.test.ts`: two different tasks at the same page differing only by host case + `:443` + `#fragment`, same version → count **1**; distinct path (`/Page`) and distinct query (`?ref=ai`) → **2**; case-different listings → **2** (opaque); `not-a-url` → throws `/not a valid URL/`; `ftp://…` → throws `/http\(s\) URL/`.

2. **Failure preservation (4053596298, P2) — `citation-panel.ts`.** `slotOutcome` no longer lets the early protocol-deviation return overwrite a real quality outcome. A `failed`/`truncated` answer now returns its own status even under a protocol deviation or a wrong prompt binding (failures/truncations are not under-reported as mere `protocol_deviant`); they are never `complete`, so surfacing them inflates no eligible denominator. A `complete` answer is promoted only when the methodology matches AND it is bound to the slot's exact `promptId`/`promptRevision`; otherwise it stays `protocol_deviant` (wrong-prompt/deviant complete answers never become eligible). Regressions: failed+deviant → `failed`; truncated+wrong-prompt → `truncated`; complete+deviant and complete+wrong-prompt → `protocol_deviant`; clean complete → `complete`.

3. **Collection-location lock (4053596302, P2) — `citation-panel.ts`.** `panelProtocolSchema` gains an approved `collection` methodology (`country`, `city`, `devicePermission`, `vpn`), and `protocolDeviations` now compares each capture's collection country/city, device-location permission and VPN state against it with strict equality — mirroring the model-label/web-search dimensions — so a known change breaks comparability and an unknown/null where the methodology pins a definite value never invents a match (`collection_country_differs`, `collection_city_differs`, `device_location_differs`, `vpn_differs`). `null` in the methodology means "pins none"; `null`/`unknown` on a capture means "not recorded" and is never fabricated into a known location. `inQuestion` is governed by the question text and is deliberately not part of the collection lock. Fixtures updated: `panel()` gains `collection` matching the clean `context()` so existing `toEqual([])` assertions hold; new regression test covers each known/unknown change and a deviant-location complete answer downgrading to `protocol_deviant`.

4. **Retest gate stability (4053596307, P2) — `citation-panel.ts`.** `comparablePairs` no longer gates on `Math.max(all verification times)`. It now requires **two distinct substantive changes verified strictly before each follow-up** via `distinctChangesVerifiedBefore(followUpAt)`; a global `totalDistinctChanges < 2` still short-circuits with `two_verified_improvements_required`. Baseline binding and chronology (each change verified after its own baseline) are preserved, distinctness collapses clones/duplicates, and a later third improvement recorded after the follow-up can no longer retroactively invalidate an already-comparable historical pair. Regressions: two changes verified before the follow-up → comparable, still comparable after adding a third distinct change verified afterwards; only one distinct change before the follow-up → `follow_up_before_both_improvements`; two clones of one change before + a distinct change after → `follow_up_before_both_improvements` (duplicates never meet the gate).

5. **GSC report timezone (4053596309, P2) — `native-ai-report.ts`.** The snapshot object is extracted to a base `nativeReportSnapshotObject`; `nativeReportSnapshotSchema` and `nativeReportImportInputSchema` both apply an `enforceReportTimezone` refine that requires a `gsc_generative_ai_search` snapshot's `period.timezone` to equal `GSC_REPORT_TIMEZONE` (`America/Los_Angeles`, spec §3.1/[G1]); a foreign or `null` GSC timezone is rejected at both the stored-record and import boundaries. Bing's export timezone is unknown until observed, so `bing_ai_performance` is not forced — its declared timezone (including `null`) is preserved and never guessed. Regressions: GSC with `null` or `Europe/Stockholm` → throws `/dated in America\/Los_Angeles/` (snapshot and import); GSC with the fixed timezone → passes; Bing `null` and Bing declared → pass.

Test inventory (structural; **NOT RUN in this session** — E2BIG persists): five new `it` blocks — four in `citation-panel.test.ts` (collection lock, failure preservation, retest-gate stability, destination identity) and one in `native-ai-report.test.ts` (GSC timezone) — each with positive and negative cases. The `panel()` fixture gained a `collection` block; existing panel/deviation assertions were checked by reading to remain green under it. No existing assertion was weakened; the destination-key change is identity-preserving for the well-formed references in prior fixtures, so prior dedup counts are unchanged.

Blocking uncertainty / decisions for Codex review: (a) **collection strict-equality direction** — I chose strict per-dimension equality (fail-closed / conservative: over-flagging reduces comparable pairs, matching the model-label precedent) rather than an asymmetric "unpinned matches anything" rule; confirm this is the intended comparability semantics for `country`/`city`. (b) **invalid-URL fail-closed = throw** — an invalid/non-http(s) `public_url` reference throws (aborting `countDistinctSubstantiveChanges`/`comparablePairs`), consistent with the existing `comparablePairs` structural throws; confirm a throw (vs. a rejected-but-non-throwing sentinel) is the desired boundary behavior. (c) These remain pure in-memory record helpers; a genuine import/storage parser must still re-establish trusted upstream statuses before any of this carries production weight. Verification, toolchain execution and release remain Codex's, under the recorded E2BIG exception.

## Third-packet integration verification — 19 September 2026

Claude completed all five new corrections at913e7212; Codex reviewed the changed logic and accepted strict collection-context matching and fail-closed invalid public destination references. Integrated released main2cc03e2f in447a7d867f3a0e0b024f0fefb9afa6742d85ad2e, without conflicts. The focused native/panel suites passed51tests/2files; full integrated suite passed6026tests/376files in58.86s. TypeScript, scoped ESLint/Prettier, production Vite/Nitro build and whitespace checks passed. Logs: /tmp/milo-pr137-third-focused-20260919.log, /tmp/milo-pr137-third-full-20260919.log, /tmp/milo-pr137-third-integrated-types-20260919.log, /tmp/milo-pr137-third-build-20260919.log.

Codex ran Claude-prepared checks under the already recorded E2BIG integration exception; Claude authored the implementation/tests/documentation. No new provider calls, exports, observations, publication, SQL or deployment occurred. These helpers remain preparation; real import/storage boundaries and D03 evidence/acceptance remain outstanding.

## Fourth external-review packet — four new findings (Claude, 19 September; prepared, NOT RUN here)

Bounded pure-module corrections for the four findings raised against the third-packet integration (base `f9a7a1517447537bb15d39104e48254ec5f824cd`). No parser, endpoint, storage, provider, migration, network, DB, deploy or credential work was added; helpers remain preparation only and new resolution surfaces stay fail-closed. No git mutation and no other-worktree write. The USD50/manual-free-account budget and accepted D03 scope are unchanged. Owned files only: `native-ai-report.ts`, `citation-panel.ts` and their tests/docs. **The CLI E2BIG spawn blocker persists in this session, so the suites below were NOT executed here; Codex runs them under the recorded exception.** Baseline/retest logic, canonical destination identity, failure-outcome preservation and the strict collection-location lock are untouched.

Files changed: `src/lib/native-ai-report.ts`, `src/lib/citation-panel.ts`, `src/lib/native-ai-report.test.ts`, `src/lib/citation-panel.test.ts`, `docs/CITATION-INTELLIGENCE-NATIVE-IMPORT.md`, `docs/CITATION-INTELLIGENCE-PANEL-AND-REVIEW.md`. `citation-finding.ts` needed no change for these four.

1. **Snapshot scope identity omits aggregation (4053687909, P1) — `native-ai-report.ts`.** `nativeSnapshotScopeKey` now includes `snapshot.aggregation` in both the `Pick` and the serialized key. The schema lets `aggregation` (`property`/`page`/`query`/`unknown`) vary independently of the report kind and dimension, so two exports that agreed on source, property, kind, dimension, period, timezone, market scope and filters but differed in aggregation previously shared one scope key and one could silently supersede the other despite being an incompatible measurement (a property-aggregated total is not a page-aggregated total). Regression (folded into the versioning test): a page-table snapshot re-aggregated at the property level (`aggregation: "property"`, all else equal) yields a *distinct* scope key, while the same-scope reimport with identical aggregation still matches.

2. **Loose period dates (4053687911, P2) — `native-ai-report.ts`.** The period boundary type `isoDate` kept only the `^\d{4}-\d{2}-\d{2}$` shape, so `2026-99-99` parsed and no ordering was enforced. It now `.refine`s each boundary to a real Gregorian calendar date via a pure `isRealCalendarDate` (rejects month `00`/`13`, day `00`, `2026-02-30`, and — by the leap rule — `2027-02-29` while allowing `2028-02-29`; no `Date` is constructed so no timezone is consulted), and the extracted shared `nativePeriodSchema` `.refine`s the period ordered `start <= end` (inclusive single-day allowed; lexical comparison of fixed-width ISO dates). Because the period schema is shared, both the stored-record (`nativeReportSnapshotSchema`) and import (`nativeReportImportInputSchema`) boundaries reject an impossible or reversed period; the source-timezone contract is unchanged and still separate. New regression `it` exercises impossible/leap/ordering/inclusive cases at both boundaries.

3. **Interface language recorded but not locked (4053687914, P2) — `citation-panel.ts`.** `panelProtocolSchema` gains a required `interfaceLanguage` (mirroring `questionLanguage`; the capture already records `language.interface` as required non-null text, so a nullable "pins none" would be unsatisfiable and a required strict-equality lock is the consistent choice). `protocolDeviations` now compares `context.language.interface` to it by strict equality and pushes `interface_language_differs`; the prompt- and interface-language locks are independent and an interface language recorded as unknown never becomes the locked expectation. The `panel()` fixture gains `interfaceLanguage: "en"` (matching the clean `context()`), so existing `toEqual([])` assertions hold. New regression `it` covers a differing and an unknown-recorded interface, independence from the prompt-language lock, and a deviant-interface complete answer downgrading to `protocol_deviant`.

4. **Arbitrary brand run accepted (4053687916, P2) — `citation-panel.ts`.** `brandRunSchema` gains an `id` (a run without identity could not be told from a fabricated one). `protocolDeviations` and `slotOutcome` take a trusted `approvedBrandRuns: BrandRun[]` (default `[]`) — resolved server-side from storage, never asserted by the capture. A brand capture's `brandRunId` must resolve to a run in that set bound to this exact panel and version, with the capture's round inside the run's approved `rounds`; otherwise it fails closed (`brand_run_not_approved` for missing/unknown/fabricated/unapproved, `brand_run_panel_mismatch` for foreign panel/version, `brand_round_out_of_run` for an unapproved round), and a brand capture with no run stays `brand_capture_without_run`. For batch counts, `panelCounts` (default `approvedBrandRuns: []`) now requires — via `assertBrandRunBudget` — exactly one approved run for the panel/version on a brand count, caps the batch at the run's `observationBudget` and rejects any capture round beyond the run's `rounds`, so no number of individually valid-looking brand captures can overrun the owner-approved observation ceiling; a brand count with no approved run fails closed. Discovery counting/pairing and `assertCaptureSlots` are unchanged (default empty runs), so discovery and the `comparablePairs` brand refusal are unaffected; brand opt-in stays separate and nothing schedules a run or expands scope. The helpers stay pure over parsed input — a future import/endpoint boundary must supply the approved runs from trusted storage, not from client input. New regression `it`s prove a valid approved run is clean, and that an absent, unknown/foreign-id, foreign-panel/version, round-out-of-run, no-run and budget-excess case each fails closed, with discovery counts unaffected.

Test inventory (structural; **NOT RUN in this session** — E2BIG persists): one new `it` in `native-ai-report.test.ts` (real/ordered calendar dates at both boundaries) plus an aggregation assertion folded into the existing versioning `it`; two new `it`s in `citation-panel.test.ts` (interface-language lock; brand-run resolution and budget, the latter with two `it`s under one new `describe`). The `panel()` fixture gained `interfaceLanguage`; the existing brand `brandRunSchema.parse` fixture gained `id`. No existing assertion was weakened; interface language "en" is identity-preserving for prior deviation fixtures and aggregation is unchanged in prior scope-key fixtures.

Decisions/notes for Codex review: (a) **interface language required, not nullable** — chosen because the capture side is required non-null, making a nullable "pins none" strict-`!==` lock unsatisfiable; confirm a required strict lock (vs. a nullable one with a separate skip rule) is the intended semantics. (b) **brand-run trust model** — the approved runs are an explicit parameter resolved from trusted storage, mirroring `comparablePairs`' scoped evidence; the helpers deliberately do NOT authenticate a client-asserted run, and a real import/endpoint boundary must resolve runs from storage before this carries weight. (c) **brand count scoped to one run** — `ReviewedCapture` carries no `brandRunId`, so a brand `panelCounts` batch is scoped to exactly one approved run for the panel/version and bounded by its budget/rounds; per-capture run binding remains a `protocolDeviations` concern. Verification, toolchain execution and release remain Codex's, under the recorded E2BIG exception.

## Fifth external-review packet — one material finding (Claude, 19 September; prepared, NOT RUN here)

Followup to the fourth packet (`913e7212`, integrated release `f9a7a151`), which Codex validated at **55 focused tests / 2 files pass** (`/tmp/milo-pr137-fourth-focused-20260919.log`). The native aggregation/calendar/interface-lock changes were reviewed and accepted and are left untouched. One remaining material gap was probed and fixed; no new broad research or full-repo audit was performed.

**The gap (independent probe `/tmp/milo-citation-fourth-review-probe.mjs`).** The fourth packet's brand-run budget closed the count boundary but bound it to the *approved run set*, not to the *captures*: `assertBrandRunBudget` required exactly one approved run for the panel/version and counted *any* `ReviewedCapture` against it, while `ReviewedCapture` carried no `brandRunId`. So a capture belonging to a foreign run was silently attributed to the single provided approved run. Reproduced: panel brand id `…001`, approved run id `…002` (budget 1, round 1); capture `SY-B01` round 1 with `brandRunId …099`, outcome complete, ownCitation true → `panelCounts` returned `recorded 1 / eligible 1` instead of refusing. Calling the input trusted does not repair an identity lost at aggregation.

**Fix (bounded, pure `citation-panel.ts`; no parser, endpoint, storage, provider, migration, network, DB, deploy, credential, scheduler or brand-scope-expansion work added). Owned files only: `citation-panel.ts` and its test; docs updated.**

1. **Carry run identity to the count boundary.** `ReviewedCapture` gains an optional/nullable `brandRunId` (absent/null on discovery for compatibility; the required, valid run identity for brand). `ScopedCapture` inherits it, so discovery pairing is unaffected.
2. **Bind EVERY counted capture to the exact approved run.** `assertBrandRunBudget` now derives the run from the captures' own `brandRunId` rather than from whichever approved run was handed in: every brand capture must carry a run id (a run-less capture fails closed — `no approved run id`), all captures must name the *same* run (a mixed batch fails closed — `must all belong to one approved run`), and that run must resolve to an owner-approved run for this exact panel and version in the trusted set (unknown/fabricated/unapproved/foreign fails closed — `not an approved run for this panel and version`). Only then do the resolved run's `observationBudget`/`rounds` bound the batch. Because the run is picked by the captures' id, a second, larger-budget approved run in the set cannot be borrowed to legitimize captures that belong to a smaller run; legitimately distinct approved runs are counted separately, one count each, and none can ambiguously pick another's run. Discovery/brand stay separate: `assertCaptureSlots` refuses a discovery capture that carries a `brandRunId`.
3. **Run approval precedes actual capture.** `protocolDeviations`' brand block now also compares the resolved run's `approvedAt` against `context.time.capturedAt`: a run approved after the observation — or with a timestamp unorderable against it — pushes `brand_run_approved_after_capture` (independent of the round check, so both can surface). A future approval cannot retroactively authorize an earlier observation. This uses the server-resolved run; no browser-asserted approval and no new endpoint are introduced. `slotOutcome` therefore downgrades a *complete* pre-approval brand answer to `protocol_deviant`, while a `failed`/`truncated` attempt keeps its own quality status (the early quality return still precedes the deviation check — failures are never masked and never promoted to complete). Unknown/unapproved runs continue to fail closed.

Regressions (structural inventory; **NOT RUN in this session** — the CLI E2BIG spawn blocker persists, and this session used Read/Edit/Write/Glob/Grep only, with no Bash/git/DB/network/provider/credential/auto-memory access): the `brandReviewed` fixture gains `brandRunId`; the first brand `it` gains explicit before/after-approval cases (`protocolDeviations` and `slotOutcome`, including a failed pre-approval attempt preserved as `failed`); the budget `it`'s no-run assertion is retargeted to the new message; a new `it` proves a valid single-matched-run positive count and the missing/foreign/mixed negatives, that a batch cannot borrow a larger run's budget (budget-1 vs budget-10 runs both present), that distinct approved runs count separately, and that a discovery capture with a `brandRunId` is refused. The current legacy planned-brand-count-0 behaviour is a separate concern and is not this finding.

Decisions/notes for Codex review: (a) **approval-time check lives in `protocolDeviations`** (where `context.capturedAt` and the resolved run both exist), so eligibility is protected transitively; the count boundary stays purely identity/budget over `ReviewedCapture`. (b) **empty brand batch fails closed** — with no captures there is no run to bind, so `runIds.size !== 1` refuses it, consistent with the fail-closed posture. (c) helpers remain pure over parsed records; a real import/endpoint boundary must still resolve approved runs and the actual capture time from trusted storage before this carries production weight. Verification, toolchain execution and release remain Codex's, under the recorded E2BIG exception.

## Final binding packet integration verification — 19 September 2026

Claude's fourth packet and brand-identity follow-up were reviewed together, committedc270541e and integrated with docs-main265a at00b7af7828563c51bb43ea54e885be092565fea9. Codex independently reproduced the foreign-brand-run counting defect before the follow-up; the same probe now refuses the foreign run and accepts one correctly bound capture. Focused56tests/2files pass; full6031tests/376files pass in50.20s; TypeScript, scoped ESLint, production build and whitespace pass. Formatter-only normalization of citation-panel.ts was a minimal integration exception. Codex ran prepared checks under the recorded test-execution exception; Claude authored the functional changes and regressions. Logs /tmp/milo-pr137-final-binding-focused-20260919.log, /tmp/milo-pr137-final-binding-types-20260919.log, /tmp/milo-pr137-binding-full-20260919.log, /tmp/milo-pr137-binding-build-20260919.log.

Review scope is the delta since f9a7a151 and its critical interactions, not another broad review of unchanged code. Native aggregation/date semantics, interface language, approved brand-run identity/budget and prospective approval are corrected. These parsed-memory helpers remain preparation; no genuine export, collection, provider call, migration or production acceptance is claimed.
