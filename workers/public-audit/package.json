{
  "name": "@milo/public-audit-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist --config ./wrangler.jsonc --env=\"\"",
    "build:staging": "wrangler deploy --dry-run --outdir dist/staging --config ./wrangler.jsonc --env staging",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260728.1",
    "typescript": "^5.8.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.114.0"
  }
}
