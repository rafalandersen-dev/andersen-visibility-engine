# Native navigation controls — 12 September 2026

Prepared, unreleased on codex/milo-report-branding-authority-20260912 after1796408. Overall60%/implementation75%, weighted58.25%/73.5% unchanged; paid NO-GO.

Current TypeScript AST inventory confirmed31 direct Link/a parents with a sole Button child and no asChild across10files. Replaced these with Button asChild wrapping the native Link/a, producing one interactive element. Destinations, auth query parameters, email hrefs, hash targets, target/rel and contents are preserved. The home CTA's former wrapper inline-block class was removed so its single element retains Button inline-flex styling; margin remains. Existing formatter normalized the touched files.

Validation:47existing focused tests/six files pass, full types/build and zero changed-file lint pass. All65link attribute sets across the10files compare unchanged against1796408. The repeated AST inventory returns zero matching nested compositions. Built browser checks on home, beta, demo script, free audit, local SEO guide and terms find zero a/button nesting. Home exposes nine styled links and Enter on the in-page explainer link reaches #how. Authenticated pages were covered through source/type and applicable existing mocked UI checks, not a real signed-in browser session. This is not a full WCAG or fluent/mobile acceptance audit.

The standard vite preview command failed because it expected dist/server/server.js while this project builds a Cloudflare module. Browser checks used the existing read-only local HTTP adapter against .output/server/index.mjs and .output/public; no deploy, external account, email, audit generation or provider action. Validation artifacts /tmp/milo-native-links-{before,applied}.json, /tmp/milo-native-links-{tests,types,lint,build}.log and /tmp/milo-link-boundaries.cjs. No new tests merely mirroring JSX were added.

Remain in this task while predecessor goal-pause cleanup is unresolved. R16 proof/action/publication/observation lineage and outcome learning remain open; R22/R23 real testers, solo/team/mixed and unattended acceptance remain open. PR135 security quota HOLD, PR129 cost/picker gate, D07 unanswered and deferred Stripe remain unchanged. No new review-consuming PR, held merge or release. Report branding1796408 remains included; German/Spanish staged work is separate and no dictionary changes occurred here.
