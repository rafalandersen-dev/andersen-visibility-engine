# Ongoing backlink monitor — implementation and release record

Current status: RELEASED through PR128 atd622575eb8d2017cbe67cfd750dfb6ae8f0339bb. Two migrations applied once; exact live runtime/seven authentication checks/catalog and prior records verified. The new scheduler is active; zero owner monitor configurations/occurrences were created. [Release evidence](../evidence/backlink-recurring-release-2026-09-12.md). Earlier foundation and pre-release entries below are historical; do not reapply migrations or repeat activation. Real provider/browser/concurrency acceptance remains open.

12 September 2026: a pure settings and occurrence planner is implemented independently of the continuation release. It is not called by a route, scheduler or supplier transport. No monitor is created or enabled and no production timer, database or budget is changed.

The owner settings contract has an explicit enable state, daily/weekly UTC cadence, 1–92 complete days of aggregate new/lost observations, subdomain scope and a monthly supplier cap. Enabling requires a cap sufficient for one full bounded request; this check does not provision account/global funds or authorize collection. The current implementation bounds the configurable cap at 100 USD per monitor, with zero permitted only while paused. This is an implementation bound, not a pricing plan or a suggested spend.

Planning takes a server-saved due time, skips missed occurrences without a backlog burst, preserves the original cadence anchor, and returns the most recent due occurrence and the next due time. Date windows exclude the current partial UTC day, including at midnight and leap-day boundaries. Exact saved hostname/subdomain scope and the released daily-series price bound are preserved.

Fourteen pure fixture tests, TypeScript and changed-file lint pass for the foundation. Required next work: private settings/revision/monthly expense and occurrence storage; authenticated owner configuration; atomic due-work claims; current website/account/configuration fences; remaining monitor/account/global budget admission before dispatch; one request identity and immutable history per occurrence; paused/unknown/pending accounting holds; bounded shared-secret executor and localized controls. Existing manual monitoring lifecycle can be factored but cannot be invoked in a way that bypasses per-monitor admission. Real provider and browser acceptance remain open. No full-feature completion credit is claimed.

## Private configuration storage in progress

Unapplied migration20260912010000 currently adds a private owner/project settings table with stable monitor identity, revision checks, exact change replay and preserved due anchor. Owner/account/project/current-website checks precede writes. Settings reject unexpected authority fields, invalid windows/cadence and enabling a cap below one bounded request. Direct table access is revoked even for the service role; only owner-checking configuration functions execute through the server role. No timer or executor consumes the table yet.

Twenty-nine pure/actual-SQL tests and changed-file lint pass. Configuration replay creates no supplier request or expense record, stale edits are refused, and pausing/editing preserves identity and the due anchor. Runtime request/dispatch/period accounting, authenticated UI and real acceptance remain required before release.

## Authenticated configuration and next implementation boundary

Read/save server functions now derive the owner from authentication, validate exact settings and revision, compare the displayed website before writing, and verify the returned owner/project/website/settings/revision. Private mutation identities are stripped. Thirteen configuration service tests, TypeScript and changed-file lint pass; endpoint authentication fixtures are included before the configuration commit. No UI, due-work scan or provider executor is wired yet.

The next implementation should store one immutable occurrence per monitor/due time, with the settings revision, exact website and observation scope, old request identity, supplier billing month, reservation ceiling and confirmed actual cost. Serialize claims with the existing owner workspace lock. Index occurrence spending by monitor/month and count unresolved reservations at their full ceiling; combine that per-monitor check with unchanged account/global supplier admission. Unknown or pending accounting must hold further automatic work. A known-undispatched failure can release only its local ceiling after proving no supplier expense exists. Preserve the due anchor and skip missed periods; edits must not create backlog bursts or reset the monitor spending identity.

Only after durable reservation/dispatch/finish and accounting are implemented should a bounded shared-secret executor, schedule integration and localized owner controls be connected. The existing manual lifecycle can share transport/execution code, but recurring dispatch must pass the additional per-monitor checks. No deployment or live calls are authorized merely by a pure due plan.

## Durable occurrences and supplier admission

The unapplied migration now has two private tables and seven functions. One occurrence binds the monitor, current settings revision, website, complete UTC date scope, due/next due times, UTC billing month, reservation ceiling and the released manual-request identity. Claims retain the due anchor, skip missed intervals and refuse duplicate occurrences. Millisecond precision keeps PostgreSQL due times and the JavaScript plan identical. Project deletion removes inactive monitoring configuration and linked occurrence records through existing project ownership; global/account expense and quota controls remain separate.

The per-monitor spending projection reads immutable existing supplier receipts. A settled receipt counts actual cost. Pending or unknown accounting holds at least the original ceiling, including a greater reported cost where present, and blocks future occurrences across months. Existing accounting recovery updates the monitor calculation directly. Only an expired reserved/held request with no supplier expense record can be released as never admitted.

Recurring dispatch rechecks the settings revision, enable state, website, billing month and remaining monitor cap immediately before invoking unchanged account/global expense admission. A confirmed SQL rejection rolls back its admission subtransaction; only proof of no expense permits releasing that local reservation. A lost HTTP acknowledgement after successful admission remains dispatched/uncertain. Successful completion can retain its admitted revision even when the owner pauses meanwhile.

All 64 planner/config/endpoint/actual-SQL tests, TypeScript and changed-file lint pass (`/tmp/milo-recurring-budget-{tests,types,lint}.log`). Coverage includes exact one-time admission, actual versus reserved cost, later accounting recovery, expiry without freeing unknown charges, current-setting/website/month fences, remaining monitor cap and unchanged restricted/paused/exhausted account budgets. Due enumeration, the bounded worker, configuration spending/history views, scheduling and localized controls remain in progress. No migration, timer, budget, provider request or production record has been created.

## Integrated worker, controls and history — ready for release review

The current feature now connects authenticated owner settings, private occurrence/expense admission, the bounded worker, four-locale controls and recurring origins in existing history. Two unapplied migrations add two private tables/nine functions plus a disabled product scheduler job. No production monitor, budget, permit, supplier request or timer activation has occurred.

The scheduler claims at most two inspections per tick, ordered by oldest inspection and due time. A five-minute inspection cooldown and row locks with SKIP LOCKED prevent invalid websites or overlapping ticks from monopolizing every check. This claim records inspection time only; it does not reserve a supplier request, free costs or authorize dispatch. Actual reserve/authorize/finish operations still acquire the owner workspace lock and recheck current account/project/website/settings, complete UTC window and monitor/account/global allowance. Bounded database calls, a 15-second supplier transport and a ten-second admission margin limit worker starts; dispatched uncertainty is retained without a supplier retry. The existing shared-secret route selects only the requested backlinks engine and returns aggregate counts.

Owner controls default to paused with zero cap. They support daily/weekly cadence, exact decimal USD cap entry, complete-day window, subdomains, explicit save and immediate pause using the saved revision. Saving a cap does not fund an account. Unconfirmed saves require a successful reload before another edit. Queries are scoped to session/project/website; background focus/reconnect does not discard edits. The card displays current UTC billing-month reserved/spent costs, cap holds, unresolved accounting and website changes. Pausing prevents new dispatches; already admitted work may still finish and incur its reserved cost. Current history adds validated scheduled occurrence timing and confirmed no-dispatch labels while preserving the released immutable observations and accounting recovery.

201 focused tests across14 files pass, including actual SQL, injected worker transport, authentication/engine routing, USD precision and static rendered controls. TypeScript and changed-file lint passed for the integrated UI before the final scheduler fairness refinement; final validation is required at the reviewed source. Tests prove one provider attempt, real expense admission before injected transport, no funding from a monitor cap, unknown-charge holds, actual receipt recovery, pause/revision/month/website fences, fair bounded inspections, private table/function permissions and a disabled scheduler installation that preserves prior jobs. Static render and single-session SQL fixtures do not establish signed-in, real-provider or live-concurrency acceptance.

Release must pin all31 prior migration sources, unchanged expense/monitor/details/page function definitions and permit permissions; rehearse and apply the two new migrations once; deploy only the reviewed exact source; verify runtime/catalog/authentication and unchanged prior records. Activate only the new product scheduler job after that runtime is verified, preserving all previous jobs and owner configurations. Real supplier and required browser acceptance remain open. This implementation does not earn a new overall completion increment by itself.
