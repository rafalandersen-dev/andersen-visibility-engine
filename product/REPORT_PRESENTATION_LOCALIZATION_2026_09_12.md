# Monthly report presentation — 12 September 2026

Prepared and locally validated; unreleased. This is a deliberately separate report-only stream from the owner beta-guidance work in task `01a0948e-69bf-7931-838a-d91734e905fd`. Its branch starts from prepared beta-controls documentation `9cc1754`. Merge this branch normally into the current UI candidate after its owner finishes the beta batch; do not replace the candidate or merge unreviewed source into main.

The report screen, print view and rendered report email now display localized month/date/number text for the four current interface languages. Email headings, summary, empty states, GSC integrity copy and attribution reuse the existing English/Polish/Swedish/Danish catalogs. The email subject uses the same localized report title/month as its body. The footer reuses the report's existing localized attribution and verified-publication note. No new dictionary keys, shared catalog edits or French-runtime activation are introduced.

The screen uses the device-selected interface language. The email still uses the server-read project's `appLanguage`, which was its existing selection rule; the request does not gain a client-provided language or recipient. Integration with the separate 24-language email preference remains open and is not claimed by this batch. Client titles, URLs, agency names, imported range labels and stored data remain unchanged.

Formatting is presentation only. Month selector values and aggregation periods remain `YYYY-MM`. Dates retain their recorded calendar day, matching the old report date slices, independent of browser/server timezone; impossible dates and invalid months render unavailable instead of rolling into a different period. Numbers retain the existing two-decimal metric precision and distinguish unknown from zero. Stored values, report filtering/counting, billing entitlement checks, branding writes and email authority/provider behavior are unchanged. The HTML renderer still escapes untrusted data and allows only existing safe URL schemes. Unknown partner-link counts remain omitted in email.

Validation: 63 focused tests across five files pass, including 29 new presentation/route cases. These cover all four locales, month/date boundaries, malformed periods, leap days, unknown metrics, decimal/group separators, unavailable-language fallback, escaped names/content/URLs, unchanged input objects, canonical selector values and unchanged mocked email request data. Existing aggregation and branding tests continue to pass. Full TypeScript and production build pass. Changed-file lint has zero errors and zero new diagnostics; the report route retains its single pre-existing `react-hooks/exhaustive-deps` warning for the project dependency.

A read-only TypeScript AST comparison passes 28 captured boundaries: nine server declarations and 19 route/control/effect declarations or calls. The entire authenticated email server function is compared with the intentionally localized subject expression excluded. Stat display values are excluded from form-value checks because formatting those displayed values is the change. The recipient, validator, credentials/configuration checks, server workspace read, provider request, timeout, retry behavior and response handling otherwise match the pre-change source.

Evidence: `/tmp/milo-report-{focused,types,lint,build}.log`, `/tmp/milo-report-lint-baseline.json`, `/tmp/milo-report-boundary-comparison.json` and `/tmp/milo-report-boundaries.mjs`. No source mutation occurred during final checks. Required release CI still supplies the full suite on both supported runtimes. No real email/provider/browser acceptance, migration, deployment, PR or review request occurred. Required security review remains on the recorded PR135 quota hold. Overall remains about 60%, implementation about 75%, weighted 58.25% / 73.5%; paid launch NO-GO.
