# Product reference images — preliminary discovery, 12 September 2026

This discovery now accompanies an unreleased reference-photo preparation implementation. PR128 remains released and untouched. No key action, model invocation, funding or customer upload has occurred. Keep the existing fixed model gpt-image-2-2026-04-21.

The [official GPT Image 2 model page](https://developers.openai.com/api/docs/models/gpt-image-2) confirms that exact snapshot and text/image inputs. The [image generation guide](https://developers.openai.com/api/docs/guides/image-generation#earlier-gpt-image-models) says this model always uses high-fidelity image inputs and the input_fidelity option must be omitted. Reference-image requests use the edits endpoint. Their total cost includes input text, input images and output; the old prompt-only ceiling therefore cannot simply be reused. The documented medium1536x1024 output estimate is0.041USD before input costs. These are documentation estimates, not measured project costs or live acceptance.

The [official pricing page](https://developers.openai.com/api/docs/pricing#image-generation-models), retrieved as its Markdown representation, distinguishes Standard and Batch. Standard GPT Image2 rates are5USD per million input text tokens,8USD per million input image tokens and30USD per million output image tokens; the4/2.5/15 figures visible in a flattened HTML excerpt belong to Batch. Do not use Batch rates for the existing synchronous transport. An exact conservative input-image token bound still needs verification before a funded edits request can be admitted.

Existing source uses prompt-only1536x1024/medium/n1/WebP85/opaque/moderationauto through image-gen.server.ts. Image functions already retain generated output privately and attach it as proposed content; approval remains separate. generateArticleImageCore uses project visual knowledge, an existing result allowance, durable recovery and generateBudgetedImage. No reference bytes or IDs currently enter that pipeline.

Next implementation design: owner/project/asset-bound selection from authorized private uploaded product images; bounded actual bytes and decoded dimensions; immutable selected-image evidence; fixed edits transport with no retry or public URL fetch; extra input cost reservation and observed cost reconciliation; clear product reference selection and identity-fidelity caveats; retained proposed output through current review/publication. Load the OpenAI API key skill before API-backed implementation. Existing secure-key setup is unresolved and previously denied access must not be retried or bypassed. Read-only documentation does not require a key. No requirement here authorizes a supplier call.

## Local reference-selection foundation

An isolated branch now has strict owner/project/article-bound reference selection from uploaded private photos, change fingerprints, duplicate/count refusal, byte/dimension checks and copied-byte manifests. Generated or remote-only images cannot silently act as uploaded product references. Up to four inputs are bounded at4MiB each/8MiB total,2048-pixel edges and8Mi combined pixels. These are local input bounds, not a supplier token/cost contract or proof of full image decoding. Manifest aggregation rechecks byte hashes, dimensions, content type and scope. No authenticated endpoint, private storage download, provider transport or UI is connected yet.

22 focused tests, TypeScript and changed-file lint pass (/tmp/milo-reference-foundation-{tests,types,lint}.log). These are synthetic offline fixtures, not real provider or product-fidelity acceptance. The existing new-key choice is already approved (owner-controlled benchmark/continuation records); safe inspection found no usable local key. Picker account/project selection and local destination remain incomplete, and unchanged connector authentication must not be retried. No credential decision needs to be repeated and no secret was created or written.

The official vision documentation explicitly says its calculator does not estimate GPT Image generation/editing inputs, and its listed high-fidelity formula applies only to GPT Image1. Do not borrow those constants for GPT Image2. The calculator linked from the official generation guide also explicitly excludes input text/image tokens. The current [image edits API reference](https://developers.openai.com/api/reference/resources/images/methods/edit) documents no request-level maximum-token or maximum-cost field. These sources have not established an input-cost ceiling. Do not continue repeating the same searches or invent a price contract; expense admission remains a separate outstanding dependency.

## Connected preparation workflow — unreleased

The editor now has explicit owner-bound saved-photo loading, thumbnails, up-to-four selection, article-save persistence and an optional file check. New controls have English, Polish, Swedish and Danish text. Unsaved photo changes require saving first; local thumbnails must have exactly the same saved metadata fingerprints as the server candidates. Account/project/article/photo changes discard query and check state. Reads are explicit, uncached after unmount and have no automatic retry or focus/reconnect refresh.

Two authenticated endpoints accept only project/article IDs and selected image IDs/fingerprints. Private paths, ownership, connector data and model/expense/publication authority cannot be supplied by the caller. Checking loads the exact saved uploaded objects from the private article bucket, using existing account media leases. Storage streams are capped during transfer, including remaining aggregate capacity, chunk count and a shared 30-second deadline. Actual bytes and header dimensions are checked; the owner context is reread after downloads. Leases are released where their identity is known, including a late acquire; existing lease expiry contains lost acknowledgements. Checks return only point-in-time file facts and hashes, never bytes, private storage paths, raw context or credentials.

The draft selection is retained through the existing article Save path, without overwriting newer publication fields. The browser and server both refuse a nonempty selection before the prompt-only plan/usage/provider path. The existing prompt-only model, cost reserve, durable output and publication pipeline are preserved. No request with selected photos can silently spend the old prompt-only reserve and ignore those photos.

181 focused offline tests across9 files, TypeScript and new-module lint pass. Modified lines in existing modules are checked separately from pre-existing lint diagnostics. Tests cover ownership and input authority, stream/byte/dimension/chunk/deadline limits, private response projection, stale selections/thumbnails, account changes, save preservation, localized controls and refusal before usage admission. Logs: `/tmp/milo-reference-editor-{tests,types,lint}.log`. No signed-in browser, actual private download, provider/product-identity acceptance, schema change or deployment was performed.

Remaining R18 reference-image work: a verified conservative price contract for the fixed model's reference inputs; priced edits transport and observed expense handling; immutable generation-input evidence retained with recovered output; reference fidelity and signed-in/provider acceptance. The current workflow prepares and checks photos only. A file check is not a full raster decode, product-identity assessment, spend permit, generated result or publication approval. Overall delivery remains approximately60%, implementation75% (weighted58%/73%).
