# EU email localization — implementation, 12 September 2026

Status: implemented locally and not released. The full R20 requirement remains open. This slice adds all24 official EU languages to operational summaries, team summaries and project invitations, with an explicit email-language preference separate from app/article/market settings. The application's wider interface still has four languages. Copy coverage is not native-speaker quality acceptance or proof of delivery.

The existing English, Polish, Swedish and Danish text is retained. Twenty additional translations cover subjects, body instructions, safe navigation text, seven operational reason labels and all three invitation roles. The digest formatter uses the selected email locale and the task's time zone; HTML carries a validated language attribute and escapes text. Imported/project-supplied titles retain their existing language rather than being silently translated. Invitation language continues to use the requesting owner's saved preference; team summaries use the recipient's preference.

The existing delivery workers all accept the same strict24-code schema, so a newly selected language cannot be rejected by an overlooked invitation/team worker. No fallback to an invented language, recipient substitution, automatic retry, approval link or changed queue/financial authority was introduced. `OPERATIONAL_EMAIL_ENABLED`, `TEAM_NOTIFICATION_EMAIL_ENABLED` and `TEAM_INVITATION_EMAIL_ENABLED` are unchanged. D07 remains unresolved; no email, invitation request or opt-in was performed.

The settings interface displays24 native language names and saves a language through a new authenticated language-only action. Its database function derives the current account from authentication and preserves the row's current enabled value atomically. A new preference starts disabled. Changing language leaves existing queued messages untouched and cannot reinstate a stale opt-in from another session. The separate enable/disable action retains its activation and current-address verification checks and now saves only the enabled flag. Its dedicated authenticated database function preserves the row’s latest language atomically, so an older tab cannot overwrite a newer language selection. Opt-out still cancels only that account’s pending or leased summaries. The legacy combined setter remains compatible for existing callers; the new UI does not use it. Queries are scoped to the current account with no automatic retries/focus/reconnect refresh; failed reads hide stale controls. An uncertain save holds further changes until an explicit successful reload. The new language-only response is bounded to10 seconds and never exposes private RPC errors or retries an uncertain mutation.

Pending migration `20260912030000_eu_email_languages.sql` expands the existing preference constraint and setter to24 codes and creates `set_operational_email_language(text)` and `set_operational_email_enabled(boolean)` with authenticated execution only. It leaves existing preference values, outbox rows, queue selectors, recipients, schedules and release flags unchanged. It has been applied only to isolated PGlite fixtures, not production. Released prior migrations must not be repeated. Any eventual rollback must preserve support for new locale values already saved; do not delete or silently rewrite preferences or narrow their constraint under existing24-language data.

Validation:197 focused offline tests across7 files, TypeScript and changed-file lint pass. Actual SQL fixtures cover all24 languages, upgrade preservation, account isolation, anonymous/invalid-code refusal, preserved latest opt-out and untouched queued messages. Rendering tests cover every reason and role in all24 locales, escaping, safe destination links, date/time handling and accidental mixed scripts. Each team worker consumes all24 preferences through injected transports while retaining its existing admission/retry tests. Settings tests cover the separate language-only action, saved language independent of app language,24 options, stale failures and sign-out. Logs: `/tmp/milo-eu-email-{tests,types,lint}.log`.

Initial source `f190a40` passed repository CI34667299982:4,281 tests/301 files plus frozen dependencies, types and builds on both Bun1.3.3 and1.4.0. Code review finding3994767163 identified the stale-tab delivery-toggle overwrite described above; the correction passes111 affected tests across3 files, TypeScript and changed-file lint. Logs `/tmp/milo-eu-email-correction-{tests,types,lint}.log`. Separate Claude review34667299983 failed before producing findings, with zero model usage; this is not a clean review.

Required remaining checks: fresh exact-source repository CI and code/security review for the correction; guarded migration/release and runtime/catalog verification; translation review by fluent users, actual signed-in settings journeys and email delivery acceptance within the existing authorization boundaries. Overall delivery remains approximately60%, technical implementation75% (weighted58%/73%) until a completed milestone warrants reassessment. Native-language copy and email coverage do not complete the wider24-language interface, support, policy or launch work.

The independent reference-image work is preserved in [draft PR129](https://github.com/rafalandersen-dev/andersen-visibility-engine/pull/129), source `dc202ceee0fd64f8d63cbb8f50e64c69bd7ed541`. Its CI34666092747 passed4200 tests/304 files, frozen dependencies, types and builds on both Bun1.3.3 and1.4.0. Automated review34666092739 failed before producing a usable review. No merge/deployment or reference-based model call occurred. Its verified input-cost contract, priced edits transport, retained generation-input evidence and real fidelity acceptance remain open; do not repeat exhausted pricing searches or reuse the prompt-only expense reserve.
