# Editor interface localization — first extraction

This branch continues from released PR131 (`18fd44a91432fa6940619c7267457d840bcf1f22`) and canonical main32145aa through a normal merge in a separate local checkout. It adds 28 `editorScreen` messages for layout tabs, content/metadata fields, regeneration labels, save/export controls, copy/delete notifications and the empty/deletion descriptions. The four current interface languages use the new copy; matching French remains staged. Existing Save wording reuses `common.save`.

The empty-state instruction is one complete sentence so each language can use its own word order. Deletion copy keeps the linked opportunity distinction and directs the user to check Plan; it no longer promises that every scheduled-publication cancellation succeeded when the existing cancellation request is best-effort. This changes explanatory copy only. Draft values, generated content, exports, event handlers, publication checks, schedule cancellation and all authority remain unchanged. Ten editor date displays now receive the selected interface language. Optional locale support in shared date helpers translates calendar text while retaining UTC audit dates, browser-local schedule dates, 24-hour clocks, invalid-date fallback and original English output for unmigrated callers.

The effective source now contains 2,730 keys in each current language; staged French covers all 2,730 across 13 batches. This is dictionary coverage, not full editor or French interface completion. Most source/image/link-safety panels and conditional messages remain to be extracted. The initial static editor inventory contained 126 candidates and excluded several dynamic labels.

Validation: 88 checks across the localization suites and editor-form tests pass, full TypeScript passes, and changed-file lint passes, including the actual editor route. Logs: `/tmp/milo-ui-editor-layout-{tests,types,lint}.log`. No real generation, upload, deletion, publication, email, provider request or configuration change was performed. This editor batch has no PR or release yet. Preserve PR131's stable candidate and its independent review/release evidence; do not merge the separate PR129 reference-image draft into this work.

Date validation: all14 formatter checks pass under both Europe/Stockholm and America/Los_Angeles, including source offsets across UTC midnight and local pickers across ISO serialization. The88 localization/editor checks, full TypeScript and changed-file lint also pass. Logs `/tmp/milo-ui-editor-date-{stockholm,los-angeles,focused,types,lint}.log`. No date value, picker interpretation, schedule, export or publication authority changed. Other helper callers remain to be connected to the chosen UI language.
