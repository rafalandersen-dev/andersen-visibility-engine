# Report emails in all 24 EU languages — 12 September 2026

Prepared and locally validated; unreleased. This independent report-only follow-on starts from `3231b7c` on `codex/milo-report-email-eu-20260912`. It does not include later owner/public beta or public home/pricing work. Merge normally into the latest isolated candidate; never replace it or merge unreviewed source into main. This delivery supersedes the report-email-language gap in `REPORT_PRESENTATION_LOCALIZATION_2026_09_12.md` at implementation level only.

Monthly report email copy and date/number/subject presentation now cover all 24 canonical EU email languages. Eighteen messages per language cover report headings, empty states, counters, attribution, search summary, four distinct evidence bases and the full source/unknown/coverage/date/causality disclaimer. The four existing language catalogs supply 72 current messages; 360 new email-only messages cover the remaining twenty languages, including French. All 432 messages are for report email; no interface languages or staged French UI keys were activated or added.

The authenticated one-off report request reads only `locale` from `operational_email_preferences`, filtered by the caller's user ID, after verifying the project exists in that caller's workspace. Saved preference wins over project/interface/content/market language. An absent row defaults to English, matching the settings page. A malformed row, database error or read timeout stops before email-provider dispatch; the bounded read aborts at ten seconds and does not retry. This read neither examines nor changes delivery opt-in, writes preferences, touches the outbox, resolves another recipient nor creates schedules.

Existing account-address/configuration checks, caller workspace aggregation, branding entitlement, JWT recipient, provider endpoint/from/tags, request timeout and response behavior are preserved. The explicit report button remains the sole request path. Neither request payload nor client-controlled project data supplies email language. The four interface-language report screen and print view remain unchanged. The settings note now explicitly includes requested monthly reports alongside operational summaries and requested invitations, with matching staged French wording. It continues to say saving language does not enable or send email. The existing preference table already supports all 24 values; no migration is introduced.

Email presentation preserves recorded calendar days, canonical reporting periods, two-decimal precision, unknown-versus-zero distinctions and omission of unknown partner-link counts. Client project/content/agency text, URLs and imported range labels remain unmodified and HTML-escaped. The root HTML language attribute accepts only the registered email locale. The wording does not turn declared data sources into independent verification, sum overlapping tables, infer missing traffic as zero or claim causality/conversions.

Validation: 215 focused tests across eight files pass, including 81 new EU-copy/preference/dispatch checks. Coverage includes all 24 catalog key sets and placeholders, all four evidence bases, localized HTML/subjects/calendar/number formats, preservation of escaped client data, all 24 caller preference values with delivery disabled, missing/invalid/error/timeout reads, unchanged opt-in semantics, project/configuration/caller-address guards and a mocked dispatch proving the saved language overrides both project language and request extras. Existing report aggregation, four-language screen/print, email-language registry, settings-save and French staging checks pass. No real email, database preference read or provider request was performed; all dispatch/network/database behavior in these tests is mocked.

Full TypeScript and production build pass. Changed-file lint finishes with zero diagnostics; one new test needed formatting correction, applied after the first checks completed. No production source changed during validation. A read-only AST comparison passes nine original server declarations. The full email handler comparison excludes only the added language-read statement and the body/subject language expressions; their new caller scope and dispatch ordering are tested independently. The unchanged portion retains original authority, data, recipient and provider boundaries.

Evidence: `/tmp/milo-report-email-{focused,types,lint,build}.log`, `/tmp/milo-report-email-boundary-comparison.json` and `/tmp/milo-report-email-boundaries.mjs`. Required final candidate CI still supplies the full suite on both supported runtimes. Fluent-language review, real account preference behavior, delivered-email rendering and actual reporting acceptance remain open. No translation count or passing mock test is claimed as fluent-user acceptance.

No PR, review request, migration, deployment, preference mutation, email, provider or account operation occurred. PR135's required security review remains on the recorded quota hold. Overall remains about 60%, implementation about 75%, weighted 58.25% / 73.5%; paid launch NO-GO. Forward the tested commit to current successor `01a094a3-e89e-7ee1-9534-ae3a57b1e14e` for normal integration with public home/pricing work, preserving all other held candidates and user-only boundaries.
