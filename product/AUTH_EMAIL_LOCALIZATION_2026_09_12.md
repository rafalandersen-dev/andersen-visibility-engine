# Authentication emails in24languages — 12 September 2026

Prepared, unreleased after9640141 on codex/milo-report-branding-authority-20260912. New dedicated authentication-email catalog covers all24existing email language codes; existing runtime/staged UI catalogs remain3768keys and unchanged. Six messages per language cover signup/reset title, body and unsolicited-request guidance. Subjects, previews, headings, button labels, body, footer and HTML lang use one resolved locale; HTML/plain text share the same rendered component. Existing English subjects remain exact, with simplified equivalent English body guidance. Confirmation links, website and recipient links, sender/domain, provider and account/redirect parameters remain unchanged.

Signup and reset accept validated emailLanguage with backward-compatible English default. Invalid explicit codes fail before administrative/provider calls. The pre-auth UI offers a separate native24-option Email language selector, reusing existing translated label. It defaults English and remains in page memory only; changing interface language or auth mode does not change it. Sign-in hides it. It is sent solely for that request, not written to account metadata, operational preferences, content settings, or market. No existing-account lookup is introduced.

Validation:69tests/two files pass, covering actual HTML/plaintext for both templates across24languages, exact catalog coverage/placeholders, markup escaping, legacy subjects/fallback, both mocked dispatches across24languages, unsupported locale rejection and all16prior account-retention/guard regressions. Full types/build pass. All new/changed files have zero new lint; auth-email.functions.ts retains three prior no-explicit-any findings. Logs /tmp/milo-auth-localization-{tests,types,lint,build}.log. Built local browser confirms24email options, four interface options, retained German email selection across Polish/Swedish interface changes and signup→signin→reset, and Spanish selection in reset. Browser text entry returned blank before and after interface changes; typed-input retention is therefore unverified by that check. No form submission or real email/account operation. The read-only local built adapter and temporary tab were stopped/closed.

These are authored translations and deterministic rendering evidence, not independent fluent-user review, inbox/client-layout acceptance or successful confirmation/recovery. Confirmation resend remains separate and unresolved, as in AUTH_RECOVERY_GUARDS_2026_09_12.md. German/Spanish UI staging remains disabled. Source catalog hashes are unchanged.

Overall60%/implementation75%, weighted58.25%/73.5%, paid NO-GO unchanged. All review-quota, provider/account, Stripe and real-use acceptance boundaries persist. No PR, held merge to main, migration or deploy. One full-goal owner remains in this task; both staged-language owners have delivered and were told to stop edits.
