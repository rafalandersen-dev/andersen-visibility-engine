# Billing presentation localization — 12 September 2026

Status: prepared and locally validated, unreleased. This batch follows the saved On-page Review, Services & Products and Project Setup work on `codex/milo-ui-audit-copy-20260912`. Required security review remains unavailable under the previously reported quota condition; no new PR, migration, checkout or deployment was requested.

The Billing screen adds 54 messages, including 24 unique plan-feature descriptions, across English, Polish, Swedish and Danish with matching staged French. Each current catalog and staged French now contains 3,177 keys across 19 French authoring batches. French remains outside the runtime and picker.

Subscription controls, failure fallbacks, market names, plan-limit summaries and add-on descriptions use the selected UI language. The free-preview label is translated; paid-plan product names retain their existing names. Exact feature-text mapping preserves shared catalog data and supports reordered feature arrays; unknown future features retain their supplied text. Country names are display-only translations: stored ISO codes, the separate `OTHER` option, billing-market derivation, price tables, limits and checkout payloads remain unchanged.

Money presentation uses the selected UI language for Polish, Swedish and Danish, preserving the selected market's amount and currency. Explicit currency codes distinguish SEK and DKK. English retains the existing representation. Formatting retains fractional values; invalid formatting input falls back to the prior money helper. The displayed cancellation timestamp remains the original server-supplied string; this batch does not reinterpret its time zone or alter cancellation scheduling.

The UI identifies the actual legacy Paddle portal and removes unsupported promises of immediate support or unconditional access through a billing period. The existing payment-readiness warning remains. The Stripe sandbox panel, owner/manual entitlements, paid-plan selection, cancellation/management links, profile saves, provider calls and authority are unchanged. No account, profile, entitlement, subscription or payment was changed during this work.

Validation: 99 focused checks across 12 files, full TypeScript, changed-file lint with zero diagnostics and production build pass. Logs: `/tmp/milo-ui-billing-{focused,types,lint,build}.log`. New presentation checks exercise market/currency preservation, decimal amounts, distinct Scandinavian currency codes, original English output, country/Other handling, feature reordering, unknown-feature fallback and unchanged shared catalog data. Existing billing hardening and agency-plan checks also pass.

Static inventory `/tmp/milo-ui-billing-embedded-inventory.json` retains three direct candidates in the route: the example billing email and Growth/Pro product names. Conditional strings and catalog-backed descriptions were reviewed separately. Server-generated error messages, route metadata, other pricing screens and real fluent/signed-in payment acceptance are outside this evidence. Required final-head review and both-runtime CI still precede release. Overall 60% / implementation 75% estimates remain unchanged (weighted 58.25% / 73.5%); paid launch remains NO-GO.
