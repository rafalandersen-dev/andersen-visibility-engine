# Report branding entitlement authority — 12 September 2026

Prepared, unreleased on codex/milo-report-branding-authority-20260912, base5a3f1a7. Overall60%, implementation75%, weighted58.25%/73.5%; paid NO-GO unchanged.

Inspection confirmed reportForCaller read subscription from the workspace document. readWorkspaceRow assembles raw workspace metadata or reads the legacy blob; it does not overlay current entitlements. Store persistence omits subscription and current batch RPC strips that key, so this is not evidence of a new save-path privilege bypass. Legacy snapshots still cannot establish current billing authority. The original authenticated backfill RPC also accepts a subscription snapshot for first-time metadata; no live exploit or account inspection was attempted. Webhooks/manual owner overrides write the separate entitlements table, and report branding must use that authority.

The email report now calls resolveEntitledPlan for the authenticated caller after caller-owned project validation. It applies saved branding only to the effective Agency plan. Existing authoritative rules preserve active/manualBeta/manualComped and future scheduled cancellation; free, cancelled, past-due, expired, missing and failed reads yield unbranded reports. No shared entitlement rules, workspace schema, provider behavior, UI presentation or stored branding changed. The obsolete billing helper comment claiming shared server/DB reliance was corrected.

109 tests/four files pass, including14 new dispatch regressions using the real entitlement reader/resolver with mocked database rows and mocked email transport. Cases cover stale Agency claims, missing/stale-free snapshots for legitimate manual/paid Agency, cancellation timing, read failures, caller scope and no entitlement read for foreign projects. Types, production build, changed-file lint and whitespace pass. Logs /tmp/milo-report-authority-{tests,types,lint,build}.log. No actual account, email, provider, DB mutation, migration, review request or deployment. This does not complete R16 proof/outcome learning or R22/R23 real acceptance.

Continue in the current task pending predecessor goal-pause cleanup requested by the user. Recheck the existing nested Link/a→Button inventory before accessibility cleanup. PR135 security quota HOLD and PR129 cost/picker gates remain unchanged; PR134 production stays verified without repeat SQL/deploy. German/Spanish remain incomplete staged-only. D07 unanswered, Stripe deferred, USD5 unused; all prior authorization boundaries persist.
