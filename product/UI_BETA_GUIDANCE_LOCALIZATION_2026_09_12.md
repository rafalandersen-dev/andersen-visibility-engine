# Owner beta playbook guidance — 12 September 2026

Prepared and locally validated; unreleased. Source commit `605e685` on `codex/milo-beta-guidance-20260912` extends the nine-batch UI candidate at `9cc1754`. The remaining owner playbook guidance now follows the selected English, Polish, Swedish or Danish interface language. Matching French remains staged outside the runtime and picker. This batch adds 125 messages in `beta-guidance.ts` and staged `fr-beta-guidance.ts`; all catalogs now contain 3,498 keys. Combined prepared UI coverage is ten batches / 446 added messages. French has 24 authoring batches. These counts do not establish fluent-user quality or full R20 acceptance.

The language note now describes localized guidance and the independently selected EN/PL/SV outreach language. Outreach labels and bodies are preserved exactly, including their historical offer wording; the guidance requires review of current scope, availability and approvals before use. CSV headers, example, values, filename and cleanup remain unchanged. Localized tracker chips are still presentation only. Billing market identifiers, price calculations, founding prices, owner predicate, navigation and clipboard behavior remain unchanged. Static route metadata remains English.

Operator claims were reconciled with canonical product decisions before translation:

- `DECISIONS.md` (September premium scope decision) selects Stripe as the Paddle replacement. `CURRENT_STATE.md` records deferred owner setup and incomplete payment lifecycle/commercial acceptance. The guide states this explicitly; manual Billing status is not payment or launch evidence. Existing reference prices remain intact without implying an approved paid launch.
- Opportunities redirects to Plan. Demo guidance now points to Plan content priorities and current Analytics / Search Console evidence rather than the obsolete Opportunities and Analytics v2 presentation.
- Connector implementation is not verified destination acceptance. The guide requires explicit status disclosure, destination verification and publishing approval; the “architecture-ready” reassurance is removed.
- Saved audits and drafts are the demo path. New live audits/generation require separate authorization and cost checks. The guide does not authorize outreach, payments or publishing.
- The offer describes a proposed pilot scope, reviewable drafts and conditional measurement. Readiness/Milo Score cannot prove rankings, citations, accuracy or destination readiness. AI output needs business review of facts, rights and claims.
- Scorecard numbers are labeled targets, not completed results. The commercial target is pilot commitment, with payment conditional on paid-launch approval. Segment assumptions and package recommendations are framed as needs to validate.

Validation on the final source: **80 focused tests / six files**, full TypeScript, zero changed-file lint diagnostics and production build PASS. Existing mocked route/handler tests cover four-language owner/denied views, independent outreach defaults, canonical CSV output and clipboard outcomes. The unresolved-key assertion now also covers `betaGuide`; the extra test count comes from the existing parameterized French authoring coverage test, not a new copy-only regression suite. No full local duplicate suite was run; release CI must run the full suite on both supported runtimes.

Read-only TypeScript AST comparison confirms **38 original boundaries unchanged**. A final direct-text inventory finds zero remaining direct JSX playbook text below navigation, and all 125 new guidance keys appear once in the route. This inventory is scoped static evidence, not browser or full product completeness proof. Final inspection corrected the spacing between the offer label and translated paragraph before checks. No source mutation occurred during validation.

Local artifacts: `/tmp/milo-beta-guidance-{focused,types,lint,build}.log`, `/tmp/milo-beta-guidance-boundaries.mjs`, `/tmp/milo-beta-guidance-boundary-comparison.json`. Authoring-only extraction/translation inputs remain under `/tmp/milo-beta-guidance-*`; do not rerun them on completed source. The checkout's ignored dependency link points to `/tmp/milo-technical-seo-20260911/node_modules`; preserve it.

No browser, signed-in session, fluent-user review, real demo, provider call, outreach, email, payment, migration, PR or deployment occurred. Required security review on PR135 remains on the recorded quota hold; no retry or bypass was attempted. Latest production remains PR134, already verified once.

Overall remains approximately 60%, implementation approximately 75%; weighted 58.25% / 73.5%. Paid launch remains NO-GO. Next independent scope: public demo-script/beta guidance, followed by wider public/report/policy localization and actual acceptance. Terms/refunds contain old Paddle claims and a 14-day first-payment guarantee; do not silently change that commitment. Privacy/subprocessor facts still require owner/legal verification. A separate predecessor task is implementing report presentation only; coordinate through its tested commit rather than overlapping edits.
