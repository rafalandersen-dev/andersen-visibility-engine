# Public home localization — 12 September 2026

Prepared and locally validated, not released. Home source `6c4a243`; final combined source candidate `934f718` on `codex/milo-home-localization-20260912`. Overall remains approximately60%, implementation75%, weighted58.25%/73.5%; paid launch NO-GO.

The home page now shares one device-only language state across its header, hero, workflow, capabilities, pricing/acceptance statements, FAQ and footer. Its100 new messages cover EN/PL/SV/DA, with matching French staged outside runtime/picker. The prepared interface inventory is now12 batches/646 added messages,3,698 keys/catalog and26 French authoring batches. Static metadata and crawler FAQ remain English; the crawler and visible FAQ use the same message source and canonical Agency capacity. The promotional workspace image remains unchanged English artwork. Footer market-route names retain their existing labels; their destinations remain independent of interface choice.

The native picker starts English for matching SSR/hydration, restores device choice afterward and updates in place. The translated main root declares the selected language. Region suggestion text, accessible dismissal and country names follow that language while suggestion detection, saved-region keys and destinations remain unchanged. Stable FAQ IDs preserve an open disclosure when its question is translated. No market, currency, project content language, provider input, entitlement, amount or destination is changed. Payment/supplier/publishing holds and missing-evidence distinctions are preserved in all authored languages.

Five new meaningful route regressions check all four languages against unchanged destinations, one shared selector/root, no staged French, literal interpolation of a mocked Agency capacity17, translated acceptance/alt/navigation/footer text and crawler FAQ independence from device state. Initial56 focused tests/5files, types, zero changed-file lint and build pass. The initial browser check found an open FAQ closing when its translated key changed; stable IDs fix that. Seven focused home/limit tests pass afterward.

The final candidate normally merges `934e134` (including24-language report-email `eaed45f`) via `a8ed751`, prior built beta/demo evidence and acceptance-history clarification `6181885` via `69f19a2`, and public-claims dev/build clarification `24f8a47` via `934f718`. No conflict, source replacement or published-history rewrite occurred. Report-email reads only the authenticated caller's saved email locale, fails before dispatch on invalid/failed/timed-out reads and leaves recipient/opt-in/outbox unchanged. App/auth language attributes are scoped to their translated roots/portals. See the separate report-email and language-semantics records for their preserved standalone evidence.

Final combined validation:137 focused tests/7files, full TypeScript, zero home changed-file lint and production build PASS. The focused suite includes new home tests, canonical limits, catalog/French/device checks and report-email EU/dispatch integration. A TypeScript AST comparison records unchanged home destination/search/image/id/loading boundaries against the pre-localization source. Logs: `/tmp/milo-home-combined-{focused,types,build}.log`, `/tmp/milo-home-final-lint.log`, `/tmp/milo-home-boundaries.json`. These are local checks, not required final CI/review.

Built-browser smoke used the Cloudflare .output with a temporary GET-only localhost4191 Node/ASSETS adapter (`/tmp/milo-home-built-preview.mjs`), with no provider/account/environment-credential changes. All four languages update the body and shell; at390×844 each has scrollWidth390. Polish hero and Swedish footer/header were visually inspected. Local Sweden suggestion keeps `/se` while its country label translates. FAQ opens with canonical15, remains open across EN→DA after the fix, and translates its answer. Danish restores after the initial English hydration render, persists across reload and carries to `/beta` and back. Final browser error log was empty. No audit, generation, send, purchase or publication control was invoked. Restored local language EN, reset viewport, closed the temporary tab and stopped preview processes.

This is limited local interaction/layout evidence, not fluent-language review, signed-in use, real email delivery, provider acceptance or the R22/R23 solo/team/unattended beta journeys. The inherited vite-dev `node:crypto` eager-import startup gap remains separate from the verified built runtime. Pricing, case studies, market pages and wider policy/billing reconciliation remain next work. Required security review is still unavailable; no PR, review request, SQL, deployment or external send occurred.
