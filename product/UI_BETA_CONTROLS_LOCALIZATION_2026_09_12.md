# Owner beta-validation controls — 12 September 2026

Prepared and locally validated; unreleased. The owner-only beta-validation screen now uses the selected interface language for its access notice, navigation, section headings, copy/download controls, clipboard outcomes, pricing labels and tracker field/status displays. The 88 new messages cover English, Polish, Swedish and Danish, with matching French staged outside the runtime and picker. Existing common copy/success and monthly suffix messages are reused.

The separately selected EN/PL/SV outreach labels and template bodies remain unchanged. The CSV keeps its original headers, example values, filename and cleanup behavior; localized tracker chips are presentation only. Billing markets, currencies, amount calculations, founding prices and the owner predicate remain unchanged. Long playbook guidance is still English, explicitly disclosed by a localized note; this is partial interface coverage, not completion of the playbook or beta acceptance. Static route metadata also remains English.

Validation: 79 focused tests across six files pass, including 11 new route-rendering/handler regressions for all four owner/denied states, independent default outreach language, canonical CSV output under Polish UI, and Danish clipboard success/failure. The tests use mocked auth, shell, buttons and browser objects; they do not establish browser, signed-in or fluent-user acceptance. A TypeScript AST comparison confirms 38 captured original boundaries are unchanged, including all outreach objects, tracker arrays, CSV helper, owner guard, billing inputs/calculations and existing navigation targets. The first regression run found an untranslated download label; it was corrected before the final passing run.

Full TypeScript, production build and changed-file lint pass with zero diagnostics. The route had 87 existing formatting diagnostics; formatting the edited route resolves them. No production/source semantics are inferred from formatting. Logs: `/tmp/milo-ui-beta-{focused,types,lint,build}.log`. Static evidence: `/tmp/milo-ui-beta-boundary-comparison.json` and its reusable `/tmp/milo-ui-beta-boundaries.mjs` comparator. No source changes occurred during final validation. Required release CI still supplies the full suite on both supported runtimes.

The combined nine-batch UI candidate adds 321 messages; all four runtime catalogs and staged French contain 3,373 keys. French has 23 authoring batches and remains unavailable for selection. No PR, migration, deployment, outreach, real account or provider operation occurred. PR135's required security review remains on the recorded quota hold, so this candidate has not been submitted for review or merged into main.

Overall remains approximately 60%, implementation approximately 75%; weighted 58.25% / 73.5%. Paid launch remains NO-GO. Next independent work: localize the remaining beta playbook guidance and reconcile its stale operator claims against current canonical product/release decisions, preserving outreach/export/authority boundaries. Broader public pages, reports/policies, language acceptance and real beta journeys remain open.
