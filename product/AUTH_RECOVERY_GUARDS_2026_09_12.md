# Authentication recovery safeguards — 12 September 2026

Prepared, unreleased after40c56ac. Inspection confirms auth UI has signup/signin/reset but no confirmation resend. Signup uses service-admin generateLink and Milo branded Lovable email delivery. Official Supabase resend sends an existing signup confirmation (https://supabase.com/docs/reference/javascript/auth-resend); generateLink generates links for custom email delivery and can create accounts (https://supabase.com/docs/reference/javascript/auth-admin-generatelink). These are distinct paths. No repository SMTP/send-hook configuration was found establishing equivalent native-resend delivery. Do not silently swap to a different sender or implement magic-link account creation as a confirmation resend. Real resend configuration/acceptance remains open; no live inspection/account/email operation occurred.

Password recovery now checks the branded email API configuration before administrative link generation, matching signup preflight. Known absent configuration cannot issue a new undeliverable recovery link. Existing generic response when account link generation fails remains, with no account deletion. Both signup/recovery delivery errors now persist only auth_email_delivery_failed instead of raw provider error text, which may echo private email/action-link data. Provider payload, sender and normal successful response remain unchanged.

16account-retention tests pass including3new recovery-preflight and raw-diagnostic regressions; full types/build pass. Changed test has zero lint; source retains3preexisting no-explicit-any errors, and the touched preexisting formatting error is fixed. Logs /tmp/milo-auth-recovery-guards-{tests,types,lint,build}.log. No real email, credential, account or provider call. This does not complete confirmation resend, authenticated recovery acceptance, rate-limit acceptance or public launch.

Overall60%/implementation75%, weighted58.25%/73.5%, paid NO-GO unchanged. PR135security quota HOLD and other provider/release/owner limits persist. Main work remains in task01a094e4-edcb-7ca0-9341-64d08bcadbfc without a duplicate successor goal. Spanish staged delivery is included and disabled; German remains separate pending completion.
