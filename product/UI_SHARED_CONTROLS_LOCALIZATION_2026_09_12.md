# Shared interface controls — 12 September 2026

Status: prepared and locally validated, unreleased. The combined branch now adds 182 messages across six batches and contains 3,234 keys in each current catalog and staged French, across 21 French authoring batches. French remains outside the runtime and picker.

This batch adds 21 messages for shared controls and onboarding examples in English, Polish, Swedish and Danish, plus staged French. Dialog and sheet close controls, pagination, breadcrumbs, carousel navigation and sidebar controls use the selected interface language. Plan's bulk-action bar translates the selection count and actions, reusing the existing target-date label. Three onboarding placeholders use the wizard's own selected language. The Authority copy-failure fallback reuses the translated message; the copied outreach content is unchanged.

Existing component props, spread order, refs, callbacks, keyboard handlers, open/closed state and zero-selection hiding remain unchanged. Translation hooks run before conditional returns. Dialogs and sheets serve current product screens; the generic pagination, breadcrumb, carousel and sidebar primitives also receive consistent defaults for their consumers. Preparing generic components is not counted as separate completed product journeys. Brand names, language autonyms, URLs and tracking-code examples are preserved.

Validation: 103 focused localization, opportunity, onboarding-project, planning-date and workspace-revision checks across 12 files, full TypeScript and production build pass. Changed-file lint introduces no additional diagnostics: the Authority file retains 68 pre-existing formatting errors and one warning; Sidebar retains its existing React-refresh warning. The remaining changed files have no lint diagnostics. Comparison ties Authority formatting findings to its unchanged line numbers (only one line's copy was substituted) and compares other warning messages without shifted line numbers. Logs: `/tmp/milo-ui-shared-{focused,types,build}.log` and `/tmp/milo-ui-shared-lint-{baseline,final,comparison}.json`.

A local server-render smoke check with network access refused in that process confirms default labels render, supplied navigation/sidebar accessible labels still override defaults, links retain their destinations and a zero-count BatchBar renders nothing. All nine assertions pass: `/tmp/milo-ui-shared-render-smoke.log`. This is not a browser keyboard, screen-reader or signed-in acceptance result. Static inventory `/tmp/milo-ui-shared-embedded-inventory.json` finds no direct copy candidates in the seven shared components; the edited route residuals are the Milo name and URL examples.

No production record, provider call, authentication attempt, email, purchase or deployment occurred. PR135 was inspected read-only again: its reviewed source is still `3ce139cceefa90f71e9a42a7d92549ad557122ea`, the PR remains open and the security-review usage-limit comment remains the last security result. No duplicate review was requested. Required final-head review and both-runtime CI precede release. Overall 60% / implementation 75% estimates remain unchanged (weighted 58.25% / 73.5%); paid launch remains NO-GO.
