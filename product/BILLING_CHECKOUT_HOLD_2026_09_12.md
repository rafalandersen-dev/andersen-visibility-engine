# Enforce the new paid checkout hold — 12 September 2026

Prepared, unreleased on `codex/milo-billing-checkout-hold-20260912`, based on41abf1f. Overall60%, implementation75%, weighted58.25%/73.5%; paid launch NO-GO remains unchanged.

Read-only inspection found that the authenticated legacy Paddle checkout endpoint still created transactions whenever API/price configuration existed. This contradicted the accepted Stripe replacement and held new paid activation. The separate Stripe owner sandbox validates test credentials and owner authority, and never grants production entitlements; it is not a replacement commercial lifecycle.

The legacy checkout endpoint now returns a hold without reading provider credentials, dispatching a transaction or writing entitlements. Authentication and input validation remain. Status retains legacy configuration/environment information for existing readiness consumers and explicitly returns checkoutAvailable:false. No environment flag can reopen this retired transaction path; any future commercial checkout requires a separately reviewed implementation and acceptance.

Billing no longer imports or calls the Paddle checkout creator. Paid choices are disabled, show the existing localized payment-verification label, and reference the localized hold explanation. The label can wrap within the existing card width. A forced paid handler invocation only displays the hold. Existing current-plan display, free/cancellation action, owner manual controls and separately gated Stripe sandbox remain. The full legacy portal handler is unchanged: customer/subscription IDs still come from the authenticated caller's entitlement. Webhooks, entitlement rules, prices, currency/market derivation, guarantees and subscriber data are unchanged. Existing portal options may still include provider-managed subscription changes; this delivery specifically closes new application checkout and does not migrate subscribers or change provider portal settings.

Validation:64focused tests in7files pass, including8new server cases and5rendered UI/handler cases. The server matrix covers all5plans ×6markets in both sandbox and production with synthetic configured credentials/prices, checking no fetch or entitlement access. Portal cases preserve caller-scoped management/cancellation and no dispatch for unlinked/unconfigured accounts. Four-language rendered controls refuse forced paid calls while retaining cancellation; owner sandbox visibility/manual free reset remain. Existing Stripe sandbox, agency pricing/limits, presentation and hardening regressions pass. Full TypeScript, production build, changed-file lint (zero diagnostics) and diff whitespace pass. Logs `/tmp/milo-billing-hold-{tests,ui,types,lint,build}.log`. No browser layout or actual subscriber/payment lifecycle acceptance is claimed; all provider/account boundaries were mocked.

Next: complete evidence-based feature-copy reconciliation in billing.ts/billing-presentation.ts and runtime/staged translations. Agency limits include WordPress/custom and AI evaluation entitlements, but “All connectors + AI Evaluation” is broader than configured/accepted availability. White-label has actual report source/tests (`proof-report.functions.ts`, `proof-report.test.ts`, `app.report.tsx`), so inspect report scheduling/evidence before adjusting “White-label monthly proof reports”; do not remove an implemented entitlement. Shopify setup's live `shopify.intro`/`shopify.minPerms` references contradict separate approval/publication and optional permissioned catalog reads (`refresh.catalogHelp` in ProjectKnowledgePanel). Both staged-language owners flagged this; reconcile current source and notify them. No feature copy changed in this delivery.

PR135 required security-review quota HOLD and PR129 cost/denied-picker gates are unchanged. No PR, migration, deployment, provider call, real payment, account, email or other external operation occurred. PR134 remains the already verified release. Real R22/R23 solo/team/mixed/unattended acceptance and3–5testers, D07 and owner-deferred Stripe setup remain open.
